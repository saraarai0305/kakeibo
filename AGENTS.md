# AGENTS.md

**セッションの開始時、または会話が圧縮された直後に、次を読む。**
このファイルが薄いのは意図的（中身を増やすと読まれなくなる）。詳細は参照先にある。

## 最初に読むもの

1. `D:\仕事用\共通\原則.md` — 3スレッド（AEプラグイン／日本語入力／モーション自動化）
   共通の原則。同期のタイミング、書き込みの約束
2. `D:\仕事用\共通\AIに渡す原則.md` — 道具に依存しない知見の共有台帳。
   **末尾の「# 追記」を必ず確認する。** 他スレッドからこのアプリへの依頼はここに来る
   （送る側は書き込むだけで通知はできない。読みに行くのはこちら側の役目）
3. `docs\README.md` — このアプリの外部連携仕様（日報インポート形式など）
4. `D:\仕事用\共通\即時報告網.md` — 部署への「今すぐ伝えたいこと」専用の連絡網。
   書式は同ファイル冒頭を参照

## 書くとき

何かを解決したとき、他スレッド（AEプラグイン／日本語入力／モーション自動化）にも
関係するか考える。関係するなら `AIに渡す原則.md` の一番下の「追記」に、
日付と出どころを付けて足す。**他人が書いた項目は消さない・書き換えない。**
間違いは新しい項目として訂正を足す。

## この会社の仕組みについて

人間（社長）1人が複数のAIセッションを部署のように使って運用している。
役割分担・全体ルールは `D:\仕事用\会社\`（`就業規則.md` が規則、`部署\` が各役割の手順書）。
**このリポジトリは自分の持ち場。** 普段は `docs\`・`design\` を優先してよく、
共通原則が優先するのは「他スレッドとの受け渡し」に関わる部分だけ
（就業規則 第7条「席のあいだはファイルでしか繋がない」）。

## 検品などへの引き継ぎ（2026-08-17 追加）

作業内容を人間（社長）を介さず検品などへ直接届けたいときは、
`D:\仕事用\共通\即時報告網.md` に次の形式で追記する（書式の詳細は同ファイル冒頭を参照）。

```
### [yyyyMMdd-HHmmss-XXXX] YYYY-MM-DD HH:MM — from: 開発｜Text engine UI（5.6 Luna/high） — to: 検品｜測る
<本文>
```

- IDは`yyyyMMdd-HHmmss`（現在時刻）+ 任意の4桁の数字でよい（一意であればよく、厳密な乱数でなくてよい）
- `from:`は必ず「開発｜Text engine UI（5.6 Luna/high）」を使う
- `to:`は宛先の部署名（例: 検品｜測る）。複数ならカンマ区切り
- 既存の行は消さない・書き換えない（訂正は新しい項目として追加）

書くとClaude Code側の仕組みが検知し、社長が承認した宛先へ自動で届く。
確認済み・返信も同じファイルに戻ってくることがあるため、起動時に末尾を確認する対象へ含める。

## 日報連携（2026-08-13 時点）

AEプラグイン側の秘書席（Claude Code）が `D:\仕事用\会社\板\日報\YYYY-MM-DD.json` に
`mainichi.daily-report.v1` 形式で書き出す。仕様変更・追加の依頼は
`AIに渡す原則.md` の該当日付の項目（`日報JSON` で検索）を見る。
