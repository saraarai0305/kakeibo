# ヘルスケア連携（ショートカット）の作り方

アプリは `kenko|日付|歩数|就寝|起床` という1行を受け取ると記録します。
空欄は飛ばせます（歩数だけ・起床だけでもOK）。

例:
- `kenko|2026-08-02|8234||` … 歩数だけ
- `kenko|2026-08-02|||07:30` … 起床だけ
- `kenko|2026-08-02|8234|23:40|07:30` … 全部

方式は2つあります。**Bのほうが手間ゼロ**です。

※ アクションの正確な名前はiOSのバージョンで少し違います。検索窓に
「ヘルスケア」「日付」「クリップボード」と打てば出てきます。

---

# A. クリップボード方式（かんたん・アプリで1タップ）

**自動化がクリップボードにコピー → アプリを開く → 「クリップボードから取り込む」を押す。**

## レシピ1: 歩数（毎晩 自動）

ショートカットApp → オートメーション → ＋ → **時刻**（21:30・毎日・「すぐに実行」）

アクション:
1. **ヘルスケアサンプルを検索** … 種類=歩数、日付=今日
2. **統計を計算** … 合計
3. **日付をフォーマット** … 現在の日付、カスタム `yyyy-MM-dd`
4. **テキスト** … `kenko|（3の結果）|（2の結果）||`
5. **クリップボードにコピー**

## レシピ2: 起床時刻（アラームを止めたとき 自動）

オートメーション → ＋ → **アラーム**（停止したとき・「すぐに実行」）

アクション:
1. **日付をフォーマット** … 現在の日付、カスタム `yyyy-MM-dd` （→日付）
2. **日付をフォーマット** … 現在の日付、カスタム `HH:mm` （→時刻）
3. **テキスト** … `kenko|（1）|||（2）`
4. **クリップボードにコピー**

アラームを止めた時刻＝起床時刻として記録されます。ヘルスケア不要で確実。

## レシピ3: 就寝時刻（睡眠モードをオンにしたとき 自動）

オートメーション → ＋ → **フォーカス「睡眠」**（オンになったとき・「すぐに実行」）

アクション:
1. **日付を調整** … 現在の日付に **1日を加算**（就寝は「翌朝の記録」に入れるため）
   - 深夜0時を過ぎてから寝る日が多いなら、加算なしの「現在の日付」でOK
2. **日付をフォーマット** … （1の結果）、カスタム `yyyy-MM-dd`
3. **日付をフォーマット** … 現在の日付、カスタム `HH:mm`
4. **テキスト** … `kenko|（2）||（3）|`
5. **クリップボードにコピー**

---

# B. 受信箱方式（ショートカットが自動送信・アプリ表示時に取り込む）

同期に使っているGistに `inbox.txt` を置き、ショートカットがそこへ直接書き込みます。
アプリを開いている間は45秒ごと、アプリを開き直した時に受信箱を見て、取り込んだら空にします。
ショートカットはアプリを閉じた状態でもGistへ送信できますが、iPhoneのWebアプリ自体は完全終了中に読み込み処理を実行できません。

## 必要なもの

- 同期が設定済みであること（おかね → 同期）
- **Gist ID**（設定 → 同期カードに表示。コピーできます）
- **GitHubトークン**（同期を設定したときのもの。`gist` 権限だけのやつ）

> トークンをショートカットの中に置くことになります。権限は `gist` だけなので、
> 万一漏れても触れるのは自分のGistだけです。それでも嫌なら A 方式のままで構いません。

## レシピB-1: 歩数（毎晩 自動・完全自動）

オートメーション → ＋ → **時刻**（21:30・毎日・「すぐに実行」）

1. **ヘルスケアサンプルを検索** … 種類=歩数、開始日=今日
2. **統計を計算** … 合計
3. **日付を書式設定** … 現在の日付、カスタム `yyyy-MM-dd`
4. **テキスト** … `kenko|（3）|（2）||`
5. **辞書** … キー `content`（テキスト）＝ 4のテキスト
6. **辞書** … キー `inbox.txt`（辞書）＝ 5の辞書
7. **辞書** … キー `files`（辞書）＝ 6の辞書
8. **URLの内容を取得**
   - URL: `https://api.github.com/gists/ここにGist ID`
   - 方法: **PATCH**
   - ヘッダ: `Authorization` = `Bearer ここにトークン`
   - 要求の本文: **JSON** → 7の辞書

辞書を3段重ねるのが面倒ですが、作るのは一度だけです。

## うまくいったか確かめる

実行後、アプリを開いて（または45秒待って）「からだの記録 1件 取り込みました」と出れば成功。
「からだ」タブの今日の歩数にも入っています。入らない場合は「同期設定を開く」から、同期がオンで役割が「記録する」になっているか確認してください。

---

## 大事な注意

- **ホーム画面に追加したアプリと、Safariで開いたアプリは、データ置き場が別です。**
  必ずどちらか一方（ホーム画面版を推奨）だけを使ってください。
- 取り込みボタンを押し忘れても、クリップボードに残っていれば後から押せば入ります。
- **読み取り専用の端末は受信箱を取り込みません**（記録する端末が取り込みます）。
- 就寝・起床は手入力欄もあるので、自動化を作らなくても運用できます。
  まずはレシピ2（アラーム）だけ作るのがおすすめです。一番簡単で一番確実です。
