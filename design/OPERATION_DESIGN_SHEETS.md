# 操作設計票と合格条件

更新日: 2026-08-12
目的: 画面の配置より前に、操作の役割・重さ・失うもの・確認方法を固定し、誤操作と場当たり的な調整を防ぐ。

## 使い方

新しい操作、または既存操作の役割・影響度を変える修正は、まず既存の設計IDと同じ目的の票を検索してから記録する。同じ操作の票がある場合は新しい箱やIDを作らず、既存票へ追記する。実表示で合格条件を満たすまでは、他画面へ横展開・公開しない。

## DS-AUDIT-001｜現行版と設計・運用資料の整合監査

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 全画面、仕事の記録、体調分析、設定・ヘルスケア連携 |
| 目的 | コード、公開設定、設計正本、外部手順、検査条件が同じ現行仕様を指す状態にする |
| 影響度 | 高。古い手順や正本の矛盾は、次の修正で既存の再発防止を無効にする |
| 可逆性 / 失うもの | 文書・検査構造の整理。利用者データは変更しない |
| 入口 | HANDOFF → design/README.md → 正本 → 外部手順 → コード → 実表示 |
| 実行 | 版、現行手順、位置保持、UI責務、CSSトークン、検査対象を照合する |
| 中止 / 確認 | 不一致を発見したら、実装を止めて判断ログへ記録し、旧資料は履歴として隔離する |
| 合格条件 | 現行版が1つに定まり、旧方式が明示され、位置保持などの規則が矛盾せず、検査が主要画面を対象にする |

### 監査結果（2026-08-12）

- [x] `0.31.3`、公開物、Service Worker資産を一致確認した。
- [x] `private/` が公開Git管理対象でないことを確認した。
- [x] 外部手順の現行方式と旧方式を識別した。
- [x] 共通レンダー入口を単一化し、日報の実作業時間・体調分析の作業／休憩指標を自動検査へ追加した。
- [x] Codexの実表示でPC幅・390px幅の日報、テンプレート余白、体調分析の指標切替を確認した。
- [ ] 旧UIと新UIの責務統合、CSS上書き削減、Safari実機確認は次段階で行う。

### 次段階の導線・利用履歴の設計候補（2026-08-12）

- [x] 仕事・生活・見える化をトップレベルの役割として分け、既存の抽象的な「記録する」「今日を整える」と実際の利用目的のずれを解消する。
- [x] 仕事には仕事の時間割・仕事の記録、生活には支出・収入・こころとからだを置き、共通化は分離後の重複を見て判断する。
- [ ] 利用履歴は端末内・任意・消去可能とし、ページ遷移とショートカット利用を分けて記録する。再描画や同一画面内の操作は利用回数に含めない。
- [x] 実装前に `DESIGN_DECISION_LOG.md` の導線判断を更新し、ホーム・ショートカット・各入口の実表示を確認する。

### 実表示確認結果（2026-08-12）

- [x] 仕事・生活・見える化の3グループをホームに表示し、390px幅で横はみ出しなく確認した。
- [x] 仕事の時間割、仕事の記録、支出・収入、体調分析、既存ショートカットの遷移を確認した。
- [ ] 利用履歴の端末内保存・集計・消去は次段階で実装する。

| 項目 | 確認する内容 |
| --- | --- |
| ID / 対象画面 | 操作を一意に識別できる名前 |
| 目的 | 利用者が何を達成する操作か |
| 影響度 | 低・中・高。誤操作で受ける影響 |
| 可逆性 / 失うもの | 取り消せるか、何を失うか |
| 初期状態 | 最初に何を見せ、何を隠すか |
| 入口 | 説明や操作面を開くための操作 |
| 実行 | 状態を本当に変更する操作 |
| 中止 / 確認 | キャンセル、確認ダイアログ、復元方法 |
| 合格条件 | 実表示で確認する観察可能な条件 |

---

## DS-RESET-001｜初期設定に戻す

| 項目 | 内容 |
| --- | --- |
| 対象画面 | デザイン設定センター |
| 目的 | 見た目の調整値だけを初期値に戻す |
| 影響度 | 高い。利用者の調整作業を失う可能性がある |
| 可逆性 / 失うもの | 保存済みのデザイン調整を失う。`mainichi.v1` の生活データは失わない |
| 初期状態 | 保存・入出力と離れた位置に「危険な操作 / 初期設定に戻す」の小さな入口だけを表示する |
| 入口 | 入口を押すと、説明と専用の実行ボタンを表示する。入口自体では初期化しない |
| 実行 | 展開後の「初期設定に戻す」専用ボタンだけが確認ダイアログを開く |
| 中止 / 確認 | 確認ダイアログには「キャンセル」と「初期設定に戻す」を分けて表示する。キャンセルでは何も変えない |
| 近くに置く操作 | 説明文、生活データを削除しない注記、キャンセル |
| 離す操作 | 設定を保存、設定を書き出す、設定を読み込む |

### 合格条件

- [ ] 閉じた状態では、初期化のための大きな外枠や全幅の実行範囲がない。
- [ ] 入口を押しても設定値は変わらず、説明だけが開く。
- [ ] 実行ボタンは展開後だけに見え、入口とは別の押下領域である。
- [ ] 実ブラウザで閉じた状態の実行ボタンが非表示であることを確認する。
- [ ] 確認でキャンセルすると、値も画面状態も変わらない。
- [ ] 「生活データは削除しない」が読める。
- [ ] 320px幅以上の実表示で、保存・入出力と危険操作が近接して見えない。

---

## DS-SETTINGS-FILE-001｜設定ファイルの書き出し / 読み込み

| 項目 | 内容 |
| --- | --- |
| 対象画面 | デザイン設定センター |
| 目的 | 見た目の共通設定を端末間で移す |
| 影響度 | 書き出しは低い。読み込みは中程度（プレビューを置き換える） |
| 可逆性 / 失うもの | 読み込みは保存前のプレビューを置き換えるが、端末への確定保存前なら戻せる |
| 初期状態 | 「設定ファイル」グループの中に、書き出し・読み込みを並列で表示する |
| 入口 / 実行 | 各ボタンがそのまま実行入口。読み込みは保存前のプレビューであることを示す |
| 中止 / 確認 | 読み込み失敗時は既存プレビューを保つ。保存前なら初期値へ戻すことができる |

### 合格条件

- [ ] 書き出し・読み込みは同じグループで、初期設定に戻すとは離れている。
- [ ] 読み込み後、保存前は「プレビュー中」の状態であることが分かる。
- [ ] 画面幅に応じて1列になっても、役割の違う操作と混ざらない。

---

## UX-VIEWPORT-001｜再描画時の画面位置保持

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 全画面 |
| 目的 | 保存・選択・削除で、利用者が見ていた場所を失わせない |
| 影響度 | 高い。入力途中や確認中の文脈を失わせる |
| 初期状態 | 操作後も現在のスクロール位置を保持する |
| 例外 | 戻る、別ページへ進む、ホームへ戻るは先頭から開く |
| 実装責務 | 共通描画関数が位置を保存・復元する。個別画面での先頭移動は禁止 |

### 合格条件

- [ ] 保存・選択・削除・表示切替の後、同じ画面位置に留まる。
- [ ] 戻る・ページ遷移・ホームへ戻るは先頭から開く。
- [ ] フリーボード操作後も、ページ位置を維持する。
- [ ] 実表示で、長い画面の中ほどから保存・削除・選択を確認する。

## DS-WORK-LOG-001｜仕事の記録（日報）

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 独立した「仕事の記録」画面 |
| 目的 | 一日の作業・休憩・仕事の本文を残し、体調分析で生活データと比較できるようにする |
| 影響度 | 中。保存前の入力を失うと、その日の仕事の記録を失う |
| 可逆性 / 失うもの | 保存済みの日報は次回表示時に編集できる。保存前の未入力内容は保存操作で確定されるまで保持し、ページ遷移時には破棄する |
| 初期状態 | 当日の日付、空の入力欄、保存済み記録があればサマリーだけを表示する |
| 入口 | 「今日を整える」内の「仕事の記録」ボタン、またはホームのショートカット |
| 実行 | 「日報を保存」ボタン。開始・終了・休憩分を検証してから `S.workLogs[日付]` へ保存する |
| 中止 / 確認 | 戻る／ホームへ戻るで保存せず中止。入力不足は保存せず、画面位置と入力内容を保持する |
| 合格条件 | 実表示で、入力、保存、入力リセット、保存済みサマリー、同日分析への反映、PC/スマホ幅、スクロール位置を確認する |

## DS-WORK-LOG-004｜仕事の記録の作業時刻と実作業時間

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 独立した「仕事の記録」画面、体調の分析 |
| 目的 | 作業開始・終了を列中心に揃え、休憩控除後の実作業時間を入力中・保存後・分析で一貫表示する |
| 影響度 | 中。時刻の読み違いと実作業時間の誤認は日報と分析の正確性を損なう |
| 可逆性 / 失うもの | 表示と派生計算の変更。保存データの開始・終了・休憩は変更しない |
| 入口 | 「今日を整える」→「仕事の記録」、保存済み日報の再表示、体調分析の作業指標 |
| 実行 | 開始・終了・休憩を入力すると共通計算で実作業時間を表示し、保存時も同じ計算を利用する |
| 中止 / 確認 | 入力不足・休憩超過は保存せず、戻るで中止する。分析から日報データを削除しない |
| 合格条件 | 390px幅・PC幅で時刻が各列中心に揃い、休憩控除後の値が入力中・保存済み・分析で一致し、実操作とコンソールを確認できる |

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル確認ページで、390px幅とPC幅の時刻欄を確認した。
- [x] 各時刻の表示中心と入力枠中心の差分は0pxだった。
- [x] 09:00／18:00／休憩60分で実作業時間8時間0分を表示した。
- [x] 未入力は未計算、休憩超過はエラー表示と保存拒否になった。
- [x] 実データの保存は行わず、コンソールエラーなしを確認した。

## DS-WORK-LOG-003｜仕事の記録の日付表示と日報テンプレートの余白

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 独立した「仕事の記録」画面 |
| 目的 | 日付を枠内で正確に読み取り、日報本文の入力項目を区切って確認できるようにする |
| 影響度 | 中。日付の読み違いは別日の記録につながり、余白不足は入力項目の誤認につながる |
| 可逆性 / 失うもの | 表示変更のみ。日付変更・保存モデル・入力内容は変更しない |
| 入口 | 「仕事の記録」画面をCodexのローカル確認ページで開く |
| 実行 | 日付表示の中央揃えとテンプレート間隔の共通トークンを適用する |
| 中止 / 確認 | 戻る、ページ再読み込み。無効な保存はデータを書き込まずに中止する |
| 合格条件 | 390px幅・PC幅で日付が中央に見え、実装内容と品質改善の間隔が確保され、実操作・画面位置・コンソールを確認できる |

### 実表示確認結果（2026-08-12）

- [x] Codexで開いたローカル確認ページの390px幅で、日付表示が枠内中央になった。
- [x] 日報テンプレートをスクロールして確認し、「実装内容」から「品質改善」まで16pxの共通間隔を確保した。
- [x] 日付変更、入力、休憩超過の無効な保存を実操作し、保存エラーとコンソールエラーなしを確認した。
- [x] PC幅でも日付中央・ゴシック・入力欄の非重なりを確認した。

## DS-WORK-LOG-002｜仕事の記録を体調分析へ連携

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 体調の分析 |
| 目的 | 日報の作業時間・休憩時間を、睡眠・歩数・こころ・からだ・習慣と同じ日付軸で比較する |
| 影響度 | 低。分析表示の切替だけで、保存済みデータは変更しない |
| 可逆性 / 失うもの | 指標のオン／オフはいつでも戻せる。日報データは分析画面から削除しない |
| 初期状態 | 既存の分析指標に加えて作業時間・休憩時間を表示する |
| 入口 | 分析画面の指標切替ボタン |
| 実行 | 指標の表示／非表示を切り替える |
| 中止 / 確認 | 戻る、再度切替。未記録日は欠測として扱い、0分のデータを作らない |
| 合格条件 | 同じ日付の作業／休憩が正しい位置に表示され、個別切替でき、保存・切替後も画面位置を保持する |
### 実表示確認結果（2026-08-12）

- [x] ローカル実表示で「今日を整える」から独立した「仕事の記録」を開き、作業開始・終了、休憩、プロジェクト、日報テンプレートを確認した。
- [x] 390px幅の実表示で、入力欄・チェック欄・見出し・保存導線が重ならず、作業画面がゴシックで表示された。
- [x] 休憩分が作業時間を超える保存を実表示で拒否し、記録を変更しないことを確認した。
- [x] 体調分析の実表示で「作業時間」「休憩時間」を独立指標として表示し、未記録は「未記録」と表示した。
- [x] 実表示の分析グラフで、作業・休憩の表示切替を可視操作し、現在の画面位置を保持した。
- [x] 保存操作による利用者データの追加は行わず、実データを変更しない状態で確認した。

## DS-WORK-CATALOG-001｜時間割と仕事の記録の共通仕事カタログ

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れ、仕事の記録 |
| 目的 | 仕事内容・プロジェクト・優先順位・状態を一度登録し、時間割と日報で同じ仕事を選べるようにする |
| 影響度 | 中。誤った仕事を選ぶと時間割と日報の記録が分かれる |
| 可逆性 / 失うもの | 仕事カタログと時間割の追加。既存の自由入力予定・既存日報・`S.areas`・古い本文は削除しない |
| 初期状態 | 「一日の流れ」の追加フォームに、従来の予定・毎日やることに加えて「仕事を追加」を表示する |
| 入口 | 一日の流れ → 「予定を足す」 → 「仕事を追加」 |
| 実行 | プロジェクト、仕事内容、優先順位、状態、開始・終了を登録し、仕事カタログと当日の時間割へ保存する |
| 中止 / 確認 | 閉じる、戻る。入力不足・時刻不正は保存しない。危険操作としての削除は今回の主操作に置かない |
| 合格条件 | 実表示で仕事を追加し、時間割表示と日報のプロジェクト／作業選択へ同じ仕事が現れる。スマホ幅でも入力・選択が重ならない |

### DS-WORK-CATALOG-001の補足

- 仕事カタログの正本は `S.workProjects` と `S.workItems`。時間割は `workItemId` を参照する。
- 日報は `projectId` と `workItemId` を保存し、既存の `project` 文字列も互換用に保持する。
- 仕事の状態・優先順位は管理情報であり、実作業時間とは別の値として扱う。
- プロジェクト作業チェック欄は現行画面から除去する。既存の `checks` は旧データ互換のため削除しない。

### 実表示確認結果（2026-08-12）

- [x] 時間割の「仕事を追加」から、既存プロジェクトを選び、仕事内容・優先順位・状態・開始・終了を登録できた。
- [x] 登録した仕事内容が時間割に表示され、仕事の記録のプロジェクト／仕事内容ドロップダウンに同じ名前・優先順位・状態で表示された。
- [x] 仕事を選ぶと予定時間が表示され、実績の作業開始・終了は別に入力できた。
- [x] 旧プロジェクト作業チェック欄は表示されず、既存 `checks` を保持したまま新しいチェック選択を追加しない保存を確認した。
- [x] 390px幅で横はみ出しなし。日報テンプレート6項目を保存し、保存済みの記録で再表示できた。
- [x] `tools/check.ps1` の実表示スモーク検査が完了し、コンソールエラーなしだった。

### 次段階の時間割分類・優先度分類（2026-08-12）

- [x] 共通の時間軸を残したまま、「すべて」「仕事」「生活」「共通」の表示切替を行い、選択中の枠の予定だけを表示する。
- [x] 仕事枠の主操作を「仕事内容を追加する」とし、仕事カタログ登録フォームへ直接入る。
- [x] 共通枠は仕事・生活の両方に関係する項目だけを置き、仕事・生活へ重複表示しない。
- [x] 仕事一覧を「次にやる」「今すぐやる」「いつかやる・たぶんやる」「待ち」で分類し、各仕事の優先度を一覧から変更できる。
- [x] 優先度と状態を別フィールドとして表示し、既存A〜Dの互換値を失わない。
- [x] 実表示はPC幅・390px幅で、仕事枠／生活枠の切替、追加、優先度変更、保存後の再表示を確認する。

### 実表示確認結果（2026-08-12）

- [x] Codexの実表示でPC幅・390px幅の時間割、分類切替、追加フォームを確認した。
- [x] 仕事枠では生活予定を追加する入口を出さず、「仕事内容を追加する」を表示した。
- [x] 共通枠では分類を共通に固定した追加フォームを確認した。
- [x] 既存の利用者データを変更せず、優先度4分類・状態分離・日報連携をスモーク検査で確認した。

### 次段階の整理順序

1. まず時間割分類（すべて／仕事／生活／共通）、仕事追加、仕事一覧の優先度分類を実装し、実表示で合格を確認する。
2. 次に「時間を決めて追加」の選択肢、ボタン、設定を一覧化し、重複入口・意味が曖昧な文言・不要な設定を整理する。
3. 整理後は、追加対象の分類と主操作が一致し、任意設定を開かなくても基本登録が完了し、保存後に同じ画面位置を保つことを確認する。

## DS-FLOW-EDIT-002｜既存予定の優先順位・状態編集と毎日予定の全日付削除

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの既存予定編集シート、時間割 |
| 目的 | 追加済みの仕事予定を後から分類・状態変更でき、不要な毎日予定を全日付から安全に削除できるようにする |
| 影響度 | 高。優先順位・状態の誤変更は次の行動判断を誤らせ、毎日予定の削除は複数日へ影響する |
| 可逆性 / 失うもの | 予定の項目変更は再編集できる。毎日予定の削除は正本から消えるため、実行前に全日付へ影響することを明示する |
| 初期状態 | 既存予定をタップすると、名前・開始・終了・区分に加えて、仕事なら優先順位・状態を表示する |
| 入口 | 一日の流れ → 既存予定をタップ → この日の予定を編集 |
| 実行 | 保存で予定または紐づく仕事カタログを更新。毎日予定は削除ボタンで正本から削除し、全日付の表示を更新する |
| 中止 / 確認 | 閉じる・戻るは変更なし。毎日予定の削除は全日付から消える注意を表示する。自動予定は編集・削除しない |
| 合格条件 | 実表示で優先順位・状態の保存、毎日予定の全日付削除、最大3件の同時進行表示、PC/390px幅、画面位置保持、コンソールを確認する |

### 実表示確認結果（2026-08-12）

- [ ] 既存の仕事予定の編集シートで優先順位・状態を変更し、保存後に再表示する。
- [ ] 毎日予定を削除すると、当日だけでなく別日にも表示されなくなることを確認する。
- [ ] 同時間帯の予定を3件表示し、名前・時刻が横並びで読めることを確認する。
- [ ] 390px幅・PC幅で編集・削除・重複表示が重ならず、操作後の画面位置と既存データを確認する。

### 実表示確認結果（2026-08-12・実装後）

- [x] Codexのローカル実表示で既存予定の編集シートを開き、優先順位・状態の選択欄を確認した。
- [x] `tools/check.ps1` の実ブラウザ検査で、同時間帯3件の3列表示と毎日予定の全日付削除ラベルを確認した。
- [x] 390px幅の編集シートで、入力・選択・削除・保存の主操作が重ならないことを確認した。
- [ ] iPhone正本の3件を実際に削除し、同期後のPCで全日付から消えたことを確認する。PCからは削除しない。

## DS-FLOW-VIEW-003｜時間割の分類統合・枠内表示・編集シート位置

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの時間割、予定編集シート |
| 目的 | 仕事・生活・共通の表示意図を一致させ、スマホでも予定と編集項目を枠内に保つ |
| 影響度 | 高。表示の取り違えや時刻のずれは、次の行動と予定実績の記録を誤らせる |
| 可逆性 / 失うもの | 表示モードと編集レイアウトのみを変更し、予定データは変更しない |
| 初期状態 | 「仕事」「生活」「共通」を表示し、共通は全予定を表示する。仕事・生活では選択中の見出しだけを表示する |
| 入口 | 一日の流れ → モード選択、または予定カードをタップ → この日の予定を編集 |
| 実行 | モード切替で共通レンダーを再描画。編集シートでは開始・終了を同じ2列グリッドで表示する |
| 中止 / 確認 | 閉じる・戻るは保存しない。表示切替は現在位置を保ち、予定保存・削除も位置保持契約に従う |
| 合格条件 | PC幅・390px幅で3モードの見出し、全予定表示、枠内カード、中央シート、開始・終了の揃い、位置保持、コンソールを確認する |

### 原因と再発防止

- 原因は、2区分の固定見出し・旧列CSS・下端寄せシート・標準入力幅依存がそれぞれ別経路で残っていたこと。
- 表示モードを親属性に集約し、見出し・区切り・カード幅を同じ構造から決める。時間入力は専用2列グリッドに固定する。
- 実表示では、仕事→生活→共通の順に切り替え、予定タップ、390px幅、PC幅、保存後の位置を同じ検査で確認する。

### 実表示確認結果（2026-08-12）

- [x] 仕事表示は仕事だけ、生活表示は生活だけ、共通表示は全予定になることを確認する。
- [x] 基本予定が時間割枠内に収まり、3件重複時も各カードが見えることを確認する。
- [x] 編集シートが中央に表示され、開始・終了の欄が揃うことを確認する。

### 実表示確認結果（2026-08-12・再発防止確認）

- [x] `0.31.8` でHTML・Service Worker・CSS/JSクエリが一致し、同一版キャッシュで旧4ボタン表示へ戻らないことを確認した。
- [x] 編集シートをbody直下へ移しても、閉じる操作が共通イベント委譲で動作することを確認した。

## DS-MONEY-OUTLOOK-002｜月次分析と全財産の内訳表示

| 項目 | 内容 |
| --- | --- |
| 対象画面 | お金の分析、お金の見通し |
| 目的 | 暦月単位の分析と、借入・現金・口座ごとの資金内訳を同時に確認する |
| 影響度 | 高。期間や借入の混同は、支払い判断と返済義務の認識を誤らせる |
| 可逆性 / 失うもの | 表示・集計方法のみを変更し、支出・収入・借入・口座データは変更しない |
| 初期状態 | 現在の暦月を表示し、前月・次月へ移動できる。内訳は金額表示の状態に従う |
| 入口 | 生活 → お金の分析、または生活 → 支出・収入 → お金の見通し |
| 実行 | 月境界で支出・収入を集計し、借入合計、現金、口座ごとの残高を内訳表示する |
| 中止 / 確認 | 月移動・表示切替では記録を変更しない。借入は収入へ合算しない。金額を隠しても項目名と返済義務の説明は残す |
| 合格条件 | PC幅・390px幅で月見出し・月移動・内訳・一括表示切替が読め、借入が収入分析へ混ざらず、位置とコンソールを確認する |

### 原因と再発防止

- 原因は、30日間の移動窓を暦月として扱っていたことと、口座・現金・借入の内訳表示が同じ表示状態に束ねられていなかったこと。
- 月キーを `YYYY-MM` に固定し、すべての集計・移動をそのキーから計算する。内訳の金額は単一の表示切替へ接続する。
- 実表示では当月・前月・次月を移動し、借入・現金・複数口座を確認した後、表示切替で全金額が同時に隠れることを確認する。

### 実表示確認結果（2026-08-12）

- [x] 暦月の見出しと月移動が実表示で確認できる。
- [x] 借入・現金・口座ごとの残高と合計が実表示で確認できる。
- [x] 表示切替で全金額が一括して隠れることを確認する。

## DS-SYNC-READONLY-001｜iPhone正本からPCへ読み取り専用で参加

| 項目 | 内容 |
| --- | --- |
| 対象画面 | くらしの設定 → 端末データ同期 |
| 目的 | iPhone側の既存同期先を正本としてPCへ受信し、PCからはGistへ書き戻さない |
| 影響度 | 高。既存Gistへ参加するとPC側ローカルデータが置き換わるため、端末と順序を取り違えると記録を失う |
| 可逆性 / 失うもの | iPhone側データは変更しない。PC側の受信前データは `mainichi.before-sync` へ退避し、Gistの内容でPC側だけが置き換わる |
| 初期状態 | 未設定のPCでは「読み取り専用（受信のみ）」を安全側の初期選択にする。ヘルスケア同期は別グループに残す |
| 入口 | くらしの設定 → 端末データ同期を開く |
| 実行 | 役割を読み取り専用に保存し、iPhone側で使っている既存Gist IDとトークンをPC画面へ直接入力して「端末同期を設定」を押す。既存Gist参加の確認後に受信する |
| 中止 / 確認 | Gist ID空欄の読み取り専用開始は中止する。既存Gist参加前に、PC側の記録が置き換わること、iPhone側へ送信しないこと、同期前バックアップを作ることを確認する。送信可能な役割へ戻すときは追加確認する |
| 合格条件 | 実表示で読み取り専用の役割・状態が見え、既存GistからPCへ受信でき、PCからの送信が発生せず、iPhone側データを変更しない。PC幅・390px幅、コンソール、画面位置保持を確認する |

### 原因と再発防止

- 原因は、現行の設定UIに端末役割がなく、同期開始時に設定オブジェクトを置き換えて旧UIの役割状態を失う経路があったこと。
- 既存Gistへ参加する前にPC側だけを自動退避し、読み取り専用では既存Gist IDを必須にして新規作成を禁止する。
- `pullRemote` は受信専用、`pushRemote` は読み取り専用時に送信しないという責務を保ち、状態表示にも役割を含める。
- 実表示では、役割選択、既存Gist参加の確認、受信後の状態、自動送信抑止を順番に確認する。トークン・Gist IDの値自体は記録しない。

### 実表示確認結果（2026-08-12）

- [x] 未設定のPCで読み取り専用が初期選択され、設定状態も読み取り専用と表示された。
- [x] Gist ID空欄での開始を中止し、新規Gist作成へ進まないことを確認した。
- [x] 390px幅とPC幅で、説明・役割選択・入力・ボタンに重なりと横はみ出しがないことを確認した。
- [x] 役割保存後も設定グループと画面位置を保持した。
- [x] `tools/check.ps1` が版、単一レンダー入口、PWA資産、実表示遷移、コンソールエラーなしで完了した。
- [ ] 実Gistからの受信、受信後の接続確認済み表示、PCからの送信抑止は、利用者がPCへ認証情報を直接入力した後に実施する。認証情報は記録しない。

## DS-MONEY-BORROWING-001｜借入を収入と分けて残高へ反映

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 支出・収入を記録、 お金の見通し |
| 目的 | 母などからの一時的な借入を収入と混ぜずに記録し、借入を含む現在残高を確認する |
| 影響度 | 高。借入を収入として扱うと返済義務や傷病手当の見込みを誤認する |
| 可逆性 / 失うもの | 借入記録は後から削除・訂正できる設計とする。既存の支出・収入・口座残高は変更しない |
| 初期状態 | 既存の支出／収入タブに加え、独立した「借入」タブを表示する。PC読み取り専用では保存操作を禁止する |
| 入口 | 生活 → 支出・収入を記録 → 借入 |
| 実行 | 借入先・金額を入力し「借入を記録」。`S.borrowings` に保存し、見通しの「今残っているお金（借入を含む）」へ反映する |
| 中止 / 確認 | 金額が0以下なら保存しない。借入を収入へ登録しない。PCで保存しようとした場合は読み取り専用メッセージを表示する |
| 合格条件 | iPhone幅・PC幅で借入を保存・表示でき、収入合計と分離され、借入合計が現在残高へ反映される。同期後のPCでは表示だけでき、PCから書き戻さない |

### 実表示確認結果（2026-08-12）

- [ ] 実装後、iPhone幅で借入先「母」・200,000円を保存し、借入として表示する。
- [ ] 見通しで借入を含む現在残高が計算され、収入と傷病手当の見込みが増えていないことを確認する。
- [x] PC幅・390px幅で入力・タブ・一覧・見通しが重ならず、コンソールエラーがないことを確認する。
- [x] PC読み取り専用では借入を保存できず、「見るだけの端末です」と表示されることを確認する。

### 実表示確認結果（2026-08-12・実装後）

- [x] Codexのローカル実表示で「支出・収入」→「借入」を開き、借入金額・借入先・注意書き・借入合計の枠を確認した。
- [x] PC読み取り専用で200,000円・母を入力して保存操作を行い、データを変更せず読み取り専用メッセージが表示された。
- [x] 390px幅の実表示で入力欄・保存ボタン・既存記録に重なりがなく、コンソールエラーなしを確認した。
- [ ] iPhone側で実際に200,000円を保存し、同期後に「借入合計」と「今残っているお金（借入を含む）」へ反映されることは、iPhone操作後に確認する。
## DS-FLOW-LANE-004｜共通表示のレーン固定と色トークン

### 対象・目的

時間割の「仕事」「生活」「共通」表示。仕事と生活の分類を視覚的にも操作的にも一致させ、共通表示で全予定を確認しても各予定の所属レーンを失わない。

### 判断

- 仕事レーンは仕事色（紫）、生活レーンは生活色（緑）を、アイコンとラベルに同じ共有トークンで適用する。
- 共通表示は仕事・生活・共通の3グループに分けて重なりを計算する。仕事は左レーン、生活は右レーン、共通は全幅とする。
- カードは時間割シェル内を基準に位置・幅を算出し、枠外へ出さない。長い名称はカード内部で折り返す。

### 原因・再発防止

全予定を一つの重なりグループとして計算すると、仕事カードが生活側へ移動できる。アイコンだけを着色すると、ラベルとの意味対応が崩れる。したがって、描画前のレーン分割と共有色トークンを共通レンダラーの責務とし、画面別の座標補正や色の応急処置を禁止する。

### 操作・保存・位置保持

- 表示切替では選択中の日付・スクロール位置を保持する。
- カードをタップして編集しても、表示モードと時間割のレーンを保持する。
- 保存・削除・ドラッグ後も、同じレーン規則で再描画する。

### 合格条件

- [ ] Codexの実表示で、仕事のアイコン・文字が紫、生活のアイコン・文字が緑。
- [ ] 共通表示で仕事カードが仕事枠内、生活カードが生活枠内、共通カードが全幅内に収まる。
- [ ] PC幅・390px幅で横はみ出しがなく、予定名は枠内で折り返す。
- [ ] コンソールエラーがなく、カードタップから編集できる。

### 実表示結果（2026-08-12）

- [x] CodexのPC幅で、仕事カードは時間割の左レーン内、生活カードは右レーン内に収まった。
- [x] 390px幅でも同じ境界を維持し、仕事 `rgb(121,106,168)`・生活 `rgb(79,152,111)` のアイコンとラベルを確認した。
- [x] 代表カードの矩形を実測し、横はみ出しなし。既存の操作・コンソール検査も通過した。
## DS-HEALTH-LOG-003｜体調記録の日付選択と保存済み項目の変更導線

| 項目 | 内容 |
| --- | --- |
| 対象画面 | こころとからだ（体調記録） |
| 目的 | 同期が翌日になっても対象日の体調データを保存・変更できるようにする |
| 初期状態 | 記録日は今日。各項目は保存済みなら値と「変更」、未保存なら入力欄と「保存」を表示する |
| 実行 | 記録日を選び、からだ・こころ・睡眠・歩数を項目ごとに保存する。選択日は保存キーとして使う |
| 日付変更 | 日付を変えたら未保存の下書きを破棄し、選択日の保存済みデータを再表示する |
| 注意表示 | 「変更」は赤文字・薄赤背景・赤系枠。削除などの危険操作とは別のソフトな注意トークンとする |
| 中止 / 位置保持 | 日付変更・保存・変更後も同じ体調画面に留まり、選択日とスクロール位置を維持する |
| 合格条件 | 翌日以降の対象日を選択して保存でき、再表示で「変更」に切り替わる。PC/390px幅、コンソール、歩数同期後の利用を確認する |

### 原因・再発防止

保存キーが当日固定で、対象日を選択するUIがなかったため、23:59同期後の後日入力ができなかった。画面日・下書き・保存キーを同じ選択日から生成し、項目別の保存済み判定を共通関数で行う。

### 実表示結果（2026-08-12）

- [x] 390px幅で記録日を翌日へ変更し、保存済み項目は赤系「変更」、未保存の歩数は「保存」と表示されることを確認した。
- [x] 自動検査で後日の日付にからだを保存し、保存後に「からだを変更」へ切り替わることを確認した。実データは変更していない。

## DS-FLOW-COLOR-005｜仕事・生活・共通の表示役割色

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れ（時間割） |
| 色トークン | 仕事=紫、生活=緑、共通=オレンジ |
| 適用箇所 | 上部の選択ボタンと時間割見出しロゴ。下部の仕事・生活レーン色は変更しない |
| 目的 | 表示モードと予定所属の意味を混ぜず、現在の視点を一目で把握する |
| 合格条件 | 生活表示はボタン・見出しロゴが緑、共通表示はボタン・見出しロゴがオレンジ、仕事レーン紫・生活レーン緑が維持される |

### 実表示結果（2026-08-12）

- [x] 390px幅で生活のボタン・時間割ロゴが緑、共通のボタン・時間割ロゴがオレンジ、仕事・生活レーン色が維持されることを確認した。
- [x] PC幅相当と390px幅の表示、コンソール、自動検査を確認した。
### 追記：既存仕事のカタログ移行

| 項目 | 内容 |
| --- | --- |
| 設計ID | DS-WORK-CATALOG-003 |
| 対象画面 | 仕事の一覧、仕事の時間割、仕事の記録 |
| 目的 | 既存のプロジェクト／自由入力予定を、元データを壊さず仕事カタログへ紐付ける |
| 影響度 | 中。登録後の優先度・状態が次の行動表示に影響するが、元の予定は保持する |
| 可逆性 / 失うもの | `workItems` の追加と予定へのID付与のみ。元のプロジェクト・予定・日報は削除しない |
| 初期状態 | 仕事一覧に未登録候補を表示し、候補ごとに登録チェック、優先度、状態を選べる |
| 入口 | 仕事 → 仕事の一覧 → 既存の仕事を登録 |
| 実行 | 選択した候補を `S.workItems` に追加し、同名の既存仕事予定へ `workItemId`／`projectId` を付与する |
| 中止 / 確認 | チェックなしは保存しない。登録前に追加内容と元データを保持する説明を表示する。読み取り専用では実行不可 |
| 合格条件 | 既存候補の表示、優先度・状態付き登録、時間割・日報への同一項目反映、重複登録防止、390px/PC幅、位置保持、コンソールを実表示で確認する |

### 原因と再発防止

- 既存の `S.areas`／自由入力予定と新しい `S.workItems` の責務が分かれていたため、仕事一覧が空になった。
- 既存データをロード時に自動変換すると意図しない追加や優先度変更が起こるため、一覧内の明示的な登録入口で候補・優先度・状態を確認してから追加する。
- 仕事の同一性は表示名ではなく `workItemId` で保持し、自動時間割・日報選択・一覧が同じカタログを参照する。
- [x] 390pxのCodex実表示で「プラグイン開発」「ワンモーション」などの候補、優先度・状態、登録導線を確認した。
- [x] `tools/check.ps1` で既存仕事の登録、仕事一覧への反映、再登録候補からの除外、既存の時間割・日報連携を確認した。
### 公開前確認の運用追記（2026-08-12）

- [x] 公開・プッシュ前にCodexのローカル実表示ページを開く。
- [x] PC幅と390px幅で、対象画面の見た目・操作・画面位置・コンソールを確認する。
- [x] 合格条件を満たすまで公開操作を行わない。公開後はキャッシュ更新と公開版の確認を別工程として行う。

### DS-WORK-CATALOG-004｜既存仕事を選ぶ／新しい仕事内容を追加する

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 仕事の時間割、仕事の一覧、仕事の記録 |
| 目的 | プロジェクトと仕事内容を分離し、既存仕事の再利用と新規仕事内容の作成を混同させない |
| 影響度 | 中。誤った仕事を複製すると時間割・一覧・日報の集計が分断される |
| 可逆性 / 失うもの | 既存選択は予定への参照追加、新規追加はカタログと予定の追加。既存プロジェクト・予定・日報は削除しない |
| 初期状態 | 仕事追加フォームに「登録済みの仕事を選ぶ」「新しい仕事内容を追加する」の入口を表示する |
| 入口 | 仕事 → 仕事の時間割 → 仕事内容を追加する |
| 実行 | 既存選択は `workItemId` を参照して予定を追加。新規追加はプロジェクト・仕事内容・優先順位・状態を入力して新しい `workItem` と予定を追加 |
| 中止 / 確認 | 閉じる・戻るは変更なし。選択なし、入力不足、時刻不正は保存しない。削除はこの操作票の主操作に含めない |
| 合格条件 | 既存選択と新規追加が別入口で理解でき、同じ仕事の重複作成がなく、仕事一覧・日報へ同じIDで反映される。390px/PC幅、位置保持、コンソールを実表示で確認する |

### 原因と再発防止

- 旧フォームが新規入力だけだったため、既存仕事を使う利用者が同名を再入力し、プロジェクト名を仕事内容として登録する余地があった。
- 入口を操作目的で分け、既存選択はID参照、新規追加だけがカタログ作成を行う構造に固定する。
- 合格確認では表示文字列だけでなく、登録前後の `workItemId` 件数、時間割カード、日報の選択候補を同じ実表示で確認する。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル実表示（PC幅）で、既存選択／新規追加の2入口、仕事一覧のプロジェクト参照枠、4つの優先度グループを確認した。
- [x] 390px相当の横幅条件は自動検査で横はみ出しなしを確認し、保存・選択の位置保持契約を通した。
- [x] `tools/check.ps1` で新規作成と既存選択のID再利用、コンソールエラーなしを確認した。
- [ ] Safari実機での表示・タップ確認は公開後に行う。

### 互換データの追加確認

- [x] 旧形式 `workItems`（`kind` 欠落）を読み込み、`task` として正規化された後も、時間割カードが仕事一覧の優先度色と一致する。
- [x] 正規化は既存の名称・優先度・状態・予定時刻を変更せず、`workItemId` の解決だけを補う。

### DS-WORK-CATALOG-005 実表示確認結果（2026-08-12）

- [x] ローカル実表示で、優先度グループが「今すぐやる」→「次にやる」→「いつかやる・たぶんやる」→「待ち」の順に並んだ。
- [x] 「今すぐやる」赤色を含む4色が、仕事一覧の枠・仕事内容行・時間割の仕事カードで一致した。
- [x] 時間割の仕事カードからプロジェクト・優先度・状態の補足文字を除き、仕事内容と時刻だけを表示した。
- [x] `tools/check.ps1`、構文、差分、コンソールエラーなしを確認した。

### DS-WORK-CATALOG-005｜優先度順と時間割カードの色連動

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 仕事の一覧、仕事の時間割、仕事の記録の候補表示 |
| 目的 | 次に取る行動を優先度順に読み取り、一覧と時間割の色を同じ意味で使う |
| 影響度 | 中。順序・色の誤りは、次に着手する仕事の判断を誤らせる |
| 可逆性 / 失うもの | 表示順・表示色・時間割カードの補足表示のみ。仕事データと優先度IDは変更しない |
| 初期状態 | 優先度配列は既存値を読み、時間割の仕事カードは仕事内容・時刻のみを表示する |
| 入口 | 仕事 → 仕事の一覧、または仕事 → 仕事の時間割 |
| 実行 | 一覧の優先度選択を変更して保存し、共通優先度色で一覧・時間割を再描画する |
| 中止 / 確認 | 選択前の値は変更しない。状態色と優先度色を混同しない |
| 合格条件 | 今すぐやるを最上位・赤で表示し、次にやるを次順位に表示する。時間割カードは仕事内容・時間のみで、一覧と色が一致する。390px/PC幅、位置保持、コンソールを実表示で確認する |

### 原因と再発防止

- 配列順と利用者が期待する行動順を別々に管理していたため、「次にやる」が「今すぐやる」より上に出ていた。
- プロジェクト色を時間割へ流用し、優先度色との対応表がなかったため、一覧と時間割が別の意味を示していた。
- 優先度ID→共通色の解決を実装上の一箇所へ集約し、既存IDを維持したまま一覧・時間割へ同じ値を渡す。
### DS-WORK-CATALOG-006｜優先度色の時間割連動と利用者向け文言

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 仕事の時間割、仕事の一覧、仕事内容追加 |
| 目的 | 重要度を時間割でも一目で把握し、新規ユーザーが開発背景を知らなくても次の操作を理解できるようにする |
| 入力 / 選択 | 仕事の優先度（今すぐやる／次にやる／いつかやる・たぶんやる／待ち） |
| 表示ルール | 優先度色は共通トークンで連動する。仕事一覧のグループは上辺と見出しの印で色を示し、仕事内容カードの左辺には強い優先度色を重ねない。時間割の仕事カードは優先度色を背景・枠へ反映し、仕事内容と時間だけを表示する |
| 利用者向け文言 | `workItemId`、同じカタログ、互換、元データなど内部事情は表示せず、「何ができるか」「次に何をするか」を説明する |
| 危険操作 / 位置保持 | 色・文言の変更は表示のみで、保存・削除・ドラッグのデータと画面位置を変えない。削除などの危険操作は既存の編集画面に隔離する |
| 合格条件 | 仕事一覧と時間割で優先度色が一致し、今すぐやるが赤いグループとして分かる。赤い縦線が連続せず、文言が新規ユーザーに通じ、390px/PC幅で枠外へはみ出さない。ローカル実表示とコンソールで確認する |

### DS-WORK-CATALOG-006 実表示確認欄

- [x] ローカル実表示で、仕事一覧の優先度グループと時間割カードの色が一致する。
- [x] 「今すぐやる」の赤は上辺・印で認識でき、内容カードの赤い縦線が連続しない。
- [x] 開発者向けの内部語を画面に表示せず、案内だけで次の行動が分かる。
- [x] 390px相当／PC幅で枠外へのはみ出し、文字の欠落、保存後の位置ずれがない。
- [x] `tools/check.ps1`、構文、差分、コンソールエラーを確認する。
- [x] `tools/check.ps1`、構文、差分、コンソールエラーを確認する。

### DS-WORK-CATALOG-007｜意味色の共通表示規則

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 意味色を持つ仕事一覧・時間割・ホーム選択肢・状態／カテゴリ選択部品 |
| 目的 | 優先度・カテゴリ・状態の意味を、左辺だけでなく薄い面色・枠・印の組み合わせで一貫して伝える |
| 正本 | 色相は既存の共通トークンを正本とし、面色は薄く、枠とアクセントで境界を補強する |
| 適用範囲 | 意味色を持つカード、選択肢、時間割項目へ適用する。説明パネル・入力欄など意味色を持たない部品は中立面のままにする |
| 位置保持 / 危険操作 | 表示スタイルのみを変更し、保存・選択・削除・ドラッグのデータと画面位置を変更しない。危険操作は既存の編集画面へ隔離する |
| 合格条件 | 同じ意味の色が画面間で一致し、灰色固定面に戻らない。文字・数字が読みやすく、390px/PC幅で枠外へはみ出さず、実表示で確認できる |

### DS-WORK-CATALOG-007 実表示確認欄

- [x] 時間割・仕事一覧・意味色付き選択肢で、面色・枠・アクセントが共通トークンと一致する。
- [x] 意味色のない中立パネルまで不要に着色されていない。
- [x] 390px相当／PC幅で、色の重なり、文字の読みやすさ、枠外へのはみ出しがない。
- [x] 保存・選択・削除・ドラッグ後の位置保持とコンソールエラーなしを確認する。

### DS-WORK-CATALOG-008｜仕事グループと入口の視認性

| 項目 | 内容 |
| --- | --- |
| 対象画面 | ホームの仕事グループと、仕事の時間割・仕事の一覧・仕事の記録の入口 |
| 目的 | 仕事というまとまりと、個別入口の階層を分け、薄い青と左帯で視認性を保つ |
| 表示ルール | 仕事グループは今より薄い青面。内側の仕事入口は中立面＋仕事色の左帯。3入口を同じ青面で連続させない |
| 合格条件 | グループと入口の境界、文字・数字のコントラストが明確で、390px/PC幅に収まり、実表示で確認できる |

### DS-WORK-CATALOG-008 実表示確認欄

- [x] 仕事グループが薄い青、3入口が中立面＋左帯で表示される。
- [x] 仕事・生活・見える化のグループ境界と入口の境界が見分けられる。
- [x] 390px相当／PC幅で視認性と枠内収まりを確認する。

### DS-WORK-CATALOG-009｜トップグループ共通の視認性

| 項目 | 内容 |
| --- | --- |
| 対象画面 | ホームの仕事・生活・見える化グループと各入口 |
| 目的 | 3グループの階層を同じ表示規則で読み取れるようにする |
| 表示ルール | グループは極薄い意味色の面、個別入口は中立面＋意味色の左帯。生活内の習慣など個別の意味色は保持する |
| 合格条件 | 仕事は薄い青、生活は薄い緑、見える化は薄い紫。文字・枠・帯のコントラストが保たれ、390px/PC幅で実表示確認できる |

### DS-WORK-CATALOG-009 実表示確認欄

- [ ] 仕事・生活・見える化のグループ面が薄く、色の濃さが揃っている。（0.31.18で撤回）
- [ ] 各グループの入口が中立面＋左帯になり、個別の意味色が残っている。（0.31.18で撤回）
- [ ] 390px相当／PC幅で視認性、階層、枠内収まりを確認する。（0.31.18で撤回）

### DS-WORK-CATALOG-009 撤回記録

- 生活・見える化への階層ルール適用と、仕事グループの追加希釈は全体のまとまりが微妙になったため撤回する。
- 0.31.17相当へ戻し、次回はグループ単位の比較実表示を先に行う。
### DS-WORK-CATALOG-010｜ホーム画面限定の表示復帰

| 項目 | 内容 |
| --- | --- |
| 対象画面 | ホーム画面の仕事・生活・見える化の入口 |
| 復帰範囲 | ホームの色面・グループ階層だけを変更前へ戻す |
| 維持する範囲 | 時間割の優先度色面・枠、仕事一覧の優先度構造、共通色トークン、データ、導線、位置保持 |
| 合格条件 | ホームは中立面＋左帯、時間割と仕事一覧は現行改善を維持し、390px/PC幅で実表示確認できる |

### DS-WORK-CATALOG-010 実表示確認欄

- [x] ホームの3グループと入口が変更前の表示に戻っている。
- [x] 時間割の優先度色面・枠が現行のまま残っている。
- [x] 仕事一覧の優先度グループ構造が現行のまま残っている。
- [x] 390px相当／PC幅で枠外、文字欠落、位置ずれがない。

### DS-WORK-LOG-004｜複数プロジェクトとスマホ時刻表示

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 仕事の記録、体調記録、一日の流れ、予定編集 |
| 操作 | 複数プロジェクトを選び、各プロジェクトの仕事内容をドロップダウンで指定する。仕事の内容は定型選択または自由入力で残す。日付・開始・終了は表示文字を共通化する |
| 保存 | `projectIds`／`workItemIds` の配列を正本とし、旧 `projectId`／`workItemId` は互換表示用に維持する。保存後も選択日・位置を維持する |
| 合格条件 | 390px相当／PC幅で複数選択、再表示、時刻の中央揃え、時間割のラベル・罫線・カードの一致、枠内収まりを実表示で確認する |

### DS-WORK-LOG-004 実表示確認欄

- [ ] 複数プロジェクトと各仕事内容を選択して保存・再表示できる。
- [ ] 「仕事の内容」の定型選択と自由入力の切り替えで値が失われない。
- [ ] 390px相当／PC幅で時間割の1時間軸と日付・時刻表示が一致する。
- [ ] 保存・選択・日付変更後も画面位置を失わず、枠外・コンソールエラーがない。

### DS-WORK-LOG-004 実装後確認（0.31.21）

- [x] 390px相当で、複数プロジェクトを2件選択し、それぞれの仕事内容ドロップダウンを操作できた。
- [x] 390px相当で、「仕事の内容」の定型選択と自由入力欄の表示切り替えを確認した。
- [x] 390px相当で、日付・作業開始・作業終了・予定編集の時刻が共通表示層で中央に揃った。
- [x] 390px相当で、時間割の時刻ラベルと予定カードが49px／時の共通軸に揃った。
- [x] PC幅で時間割の枠外なし、コンソール警告・エラーなしを確認した。
## DS-WORK-LOG-005｜仕事を選ぶの追加式UIと時間割フレーム

- 対象導線: 仕事の記録 → 仕事を選ぶ、日報の入力、時間割の表示。
- 操作設計: 追加用ドロップダウンで仕事を1件ずつ追加する。追加済みの仕事は一覧に残し、各行で仕事内容を選択できる。各行の「外す」は選択から戻す操作で、保存・削除などの危険操作と分離する。日付・開始・終了は共通フィールドシェル内で表示し、時間割は時刻列を含む共通フレーム内で表示する。
- 失敗と再発防止: チェックボックスを状態表示に使うと複数選択の確認性が下がるため、追加と選択済み一覧を分離する。透明なネイティブ入力に枠を持たせない。時刻列を親の外へ置かず、ラベル領域を予約したタイムラインへ内包する。
- 実表示合格: PC幅と390px幅で、2件以上の追加・仕事内容選択・1件だけ外す・再描画後の保持を確認する。日付・開始・終了の枠、時間割の外枠・時刻列・イベントを確認し、横スクロール・画面位置喪失・コンソールエラーがないこと。
## DS-FLOW-006｜時間割カードの開始位置と内容位置

- 対象: 一日の流れの時間割カード（仕事・生活・共通）。
- 診断: カードの時刻計算は正しいが、長い `button` カードの内容が内部配置で中央寄せになり、開始時刻とタイトルの視覚位置がずれて見える。
- 設計判断: カードの外形位置は共通時間軸、カード内は上端固定のタイトル・時間表示とする。カードの高さが変わっても内容位置は変えない。カードタップ・ドラッグ・リサイズの操作領域は内容位置と分離する。
- 合格条件: 10:00–12:00、13:30–16:00、14:10–14:20、19:30–21:00をPC/390pxで表示し、カード上端と開始時刻が一致する。カード内タイトルと時間が上端から同じ余白で始まり、カード中央へ移動しない。操作後も時刻と画面位置を失わない。

### DS-FLOW-006 実装判断（0.31.22修正）

- 共通部品: イベントカードは「外形・時間軸上の位置」「内容層」「リサイズ操作層」を分ける。
- 内容層: タイトル→時刻の順に上端固定で表示し、カードの高さによって中央へ移動させない。
- 合格確認: 仕事・生活・共通それぞれで、PC幅／390px幅の長時間・中時間・短時間カードが同じ内容位置規則になり、ドラッグ／リサイズの操作領域を失わない。

### DS-FLOW-006 実装後確認（0.31.22）

- [x] PC幅／390px幅で、長時間・中時間・短時間カードのタイトルがカード上端から8pxで揃った。
- [x] 10:00カードの上端と10:00ラベルの差が0pxで、13:30・19:30のカードも49px／時の軸に一致した。
- [x] タイトル→時刻の順序、リサイズ操作層、カード内の横収まりを確認した。
- [x] 横スクロールなしで、今回の表示修正によるコンソールエラーは確認されなかった。

### DS-FLOW-006 追補｜短時間カードの時刻表示

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの仕事・生活・共通時間割カード |
| 依頼・診断 | 30分予定の時刻がスマホ表示で見えない／下端で切れて見える |
| 実測 | DOMには時刻が存在する。カード高42px、上下padding各8px、タイトル約16.8px、時刻約13.2px、間隔3px、overflow hidden |
| 原因 | 最小カード高が本文と上下paddingの必要高さに足りず、時刻が下端へ押し出される |
| 再発防止 | 本文構造から最小高さを決め、短時間カードを10分・30分・1時間以上でPC／390px実表示確認する |
| 合格条件 | タイトルと開始・終了時刻がカード内に全文表示され、下端で切れない。位置・横収まり・コンソールエラーに回帰なし |

### DS-FLOW-006 追補 実装前確認

- [x] 最小カード高を本文・間隔・上下paddingから見直し、共通最小高50pxを適用した。
- [x] 30分予定をPC／390pxの実表示で確認し、「10:00 - 10:30」がカード内に全文表示された。

### DS-FLOW-007｜時間レールと体調数値表示の共通境界

- 対象: 一日の流れの時刻レール／カード領域、こころとからだの日付・睡眠・歩数表示。
- 設計判断: 時間軸のラベル領域と予定カード領域をDOMで分離し、カードのレーン計算はカード領域だけを基準にする。体調入力は共通の透明入力規則をそのまま流用せず、コンパクト枠の高さを明示する。保存値は共通の数字トークンで表示する。
- 原則: CSSの入れ子 `calc()` による座標補正を行わない。時間レール幅、入力高さ、数字書式は共有トークンから決め、ページごとに上書きしない。
- 合格条件: PC／390pxの共通・仕事・生活で時刻ラベルとカードに重なりがなく、体調の日付・睡眠時刻・歩数・睡眠時間が枠内に収まる。仕事の記録の開始・終了、保存・選択後の位置保持、横はみ出し、コンソールエラーに回帰がない。

### DS-FLOW-007 実装後確認（0.31.22）

- [x] 共通表示で時刻レールとカード領域が分離され、390pxでカードがレール終端より10px右から始まる。
- [x] 仕事／生活表示、PC幅でも同じ領域分離が適用され、カードの横はみ出しがない。
- [x] 体調記録の日付・睡眠時刻・保存値の表示が用途別の高さと数値トークンで揃った。
- [x] 仕事の記録の作業時間表示を保持し、通し検査でコンソールエラーなしを確認した。

### DS-FLOW-008｜時間割フレームの中央線と編集時刻の中央揃え

- 対象: 一日の流れのレーン見出し・時間割中央線・時刻ラベル、予定編集の開始／終了。
- 設計判断: レーン見出しと時間割中央線は、時刻レールを含む同一フレームの中央に固定する。カード領域だけを基準に中央線を描かない。開始／終了時刻は等幅2列グリッドと共通フィールドシェルで、各値を各枠の中央に置く。
- 再発防止: フレーム全体の装飾線は時間割フレームへ置き、カード層へ置かない。CSSの座標系が変わる場合は、見出し境界・中央線・時刻ラベル・カード左端を同じ実測表で確認する。
- 合格条件: 390px／PC幅で見出し境界と中央線のX差0px、時刻レールとカードの重なり0px。開始／終了の枠幅と値の中心が左右で一致し、横はみ出し・画面位置喪失・コンソールエラーなし。

### DS-FLOW-008 実装後確認（0.31.22）

- [x] 390px／PC幅で、レーン見出しの境界と時間割中央線のX差0px。
- [x] 仕事・生活・共通のカードが時刻レール外に収まり、横はみ出しなし。
- [x] 予定編集の開始／終了値が各枠の水平方向・垂直方向の中心に揃った。
- [x] 通し検査とコンソールエラー0を確認した。

### DS-FLOW-009｜時間レールの左端基準

- 対象: 一日の流れの時間ラベル・時間割フレーム・カード領域。
- 設計判断: 時間レールは時間割フレームの左端に固定する。親シェルにレール分の余白を持たせず、レール幅はフレーム内で一度だけ確保する。
- 再発防止: 旧CSSの `padding-left` と新しい内部レール幅を併用しない。外枠左端からの実測をPC／390pxで行う。
- 合格条件: 時間レール左端＝時間割フレーム左端、カード左端＞レール右端、中央線とレーン見出し境界のX差0px。横はみ出し・位置喪失・コンソールエラーなし。

### DS-FLOW-009 実装後確認（0.31.22）

- [x] PC／390pxの実表示で、時間レールが時間割フレーム左端に戻っている。

### DS-FLOW-009 実装後確認（0.31.22）

- [x] 390px幅で、フレーム左端＝時間レール左端、カード開始位置はレール終端より右を確認した。
- [x] PC幅でも時間レール左端、中央線、レーン見出しの座標が同じ契約になった。
- [x] 予定編集の開始／終了表示、横はみ出し、位置保持に回帰がないことを確認した。

### DS-FLOW-010｜仕事・生活カード領域の中央線

- 対象: 一日の流れのレーン見出しと仕事／生活予定の境界線。
- 設計判断: 時刻レールを含む枠全体ではなく、時刻レールを除いたカード領域を仕事・生活の2等分基準にする。カードの幅と配置計算は変更しない。
- 合格条件: PC／390pxでカード領域の左端・右端の中央に見出し境界と縦点線が一致し、仕事・生活カードの幅・時間位置に差分がない。時刻表示は左端レールに残る。

### DS-FLOW-010 実装後確認（0.31.22）

- [x] 390pxでカード領域X=86〜334の中央X=210、見出し境界、縦点線が同じX座標になっている。
- [x] PC幅でカード領域X=523.5〜786.5の中央X=655、見出し境界、縦点線が同じX座標になっている。
- [x] 仕事・生活カードの幅と時間位置を変更せず、時刻ラベルは左端レールに残り、横はみ出しがない。

### DS-FLOW-011｜中央点線と仕事・生活カードの共通余白

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの共通時間割、仕事／生活カード、レーン見出し |
| 目的 | 中央点線を仕事と生活の境界として読みやすくし、左右カードの中心側余白を同じにする |
| 初期状態 | 見出し境界と点線は一致しているが、仕事カード右端から点線まで2px、生活カード左端から点線まで5pxだった（PC実測） |
| 設計判断 | カード領域の中央を点線の基準に固定し、仕事・生活の内側余白とカード幅の減算を共通トークンで算出する。見出しと点線は同じカード領域基準を使う |
| 再発防止 | 左右で異なる+10px／+5pxのマジックナンバーを持たない。PC／390pxで見出し境界・点線・カード中心側端を同じ実測表で比較する |
| 合格条件 | 見出し境界X＝点線X＝カード領域中央X。仕事右端→点線と点線→生活左端の距離が同値。時間レール、予定時間、横収まり、位置保持、コンソールエラーに回帰がない |

### DS-FLOW-011 実装後確認（0.31.22）

- [x] PC幅で仕事右端→点線、点線→生活左端の距離が同値（実測5px／5px）。
- [x] 390px幅でも同値で、見出し境界・点線のX差0px（実測5px／5px、X=210）。
- [x] 時間レール左端、予定カードの時間位置、横はみ出し、位置保持、コンソールエラーに回帰なし。

### DS-FLOW-012｜見出し境界線と時間割中央線の描画ピクセル

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの共通時間割、仕事／生活レーン見出し |
| 依頼・診断 | 見出し下の短い縦点線と、時間割内の中央縦点線が見た目で1pxずれる |
| 実測 | PCの座標はX=655.0、390pxの座標はX=210.0で一致。ただし上はborder-right、下はborder-leftで線の塗られる側が違う |
| 原因 | 矩形の境界座標だけを測り、実際に描画される1pxの位置を合格条件に含めていなかった |
| 再発防止 | 同じ疑似要素・border方向・transform規則で上下の線を描画し、スクリーンショット上の実線Xも確認する |
| 合格条件 | PC／390pxで上の実線X＝下の実線X。見出し・中央線・カード領域中央の座標、カード余白、横収まり、位置保持、コンソールエラーに回帰なし |

### DS-FLOW-012 実装前確認

- [x] 同じ描画規則へ統一し、上のborder-rightを廃止して右端所有のborder-left疑似要素へ変更した。
- [x] PC／390pxの実表示スクリーンショットで上下の実線Xを比較し、見出し側X=655／210、時間割側X=655／210を確認した。
- [x] 右端固定で残った1px差を再修正し、上下とも同じborder-left＋translateX(-0.5px)へ統一した。390pxの実線列は上下ともX=201〜202。

### DS-FLOW-012 追加診断（次回実装前）

- [x] translateX(-0.5px)による中心Xの0.5px移動を取り除いた。
- [x] 390pxの中心X=210、PCの中心X=655と、上下の実線列・左右カード端を同じ実測表で比較し、左右余白5px／5pxを確認した。

### DS-FLOW-013｜時間割分類ボタンの3等分

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの「仕事」「生活」「共通」分類ボタン |
| 依頼・診断 | 3つの同格モードなのに、旧親グリッドが4列指定で、枠の配分が均等に見えない |
| 設計判断 | 親を3列の1frグリッドにし、3ボタンの枠・高さ・左右余白を共通化する |
| 再発防止 | 選択肢数と親グリッド列数を一致させ、PC／390pxで各ボタン矩形の幅を実測する |
| 合格条件 | 仕事・生活・共通の幅が同値で、外枠内、色・操作・横収まり・コンソールエラーに回帰なし |

### DS-FLOW-013 実装前確認

  - [x] 3ボタンの親グリッドを3等分へ変更する。
  - [x] PC／390pxの実表示で3つの矩形幅を比較する。
    - PC: 仕事・生活・共通が各約109.33px。
    - 390px: 仕事・生活・共通が各約104.33px。
    - 外枠内に収まり、コンソールの警告・エラーは0件。

### DS-FLOW-013 追加診断｜キャッシュ反映確認

  - [x] ソースと実ブラウザの適用CSSを照合し、実表示では `repeat(4, minmax(0, 1fr))` が残っていることを確認した。
  - [x] CSS／JS／Service Workerの版番号を0.31.23へ更新し、修正後のアセットを読み込ませる。
  - [x] PC／390pxで適用グリッドが3列、親枠の全幅を3ボタンで使い切ることを確認する。
    - PC: 適用グリッドは109.328px／109.328px／109.344px、親枠幅346px。
    - 390px: 適用グリッドは104.328px／104.328px／104.344px、親枠幅331px。
    - 両幅とも右側の余白はなく、コンソールの警告・エラーは0件。

### DS-FLOW-014｜予定の選択後移動・直前操作の取り消し

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れの時間割カード、予定の編集操作 |
| 依頼・診断 | 未選択カードのpointerdownが端の開始／終了変更を即時開始し、スマホで誤操作と端点の掴みにくさが起きる |
| 設計判断 | 1回目は選択のみ。選択後に全体／開始／終了の操作モードを選び、カード中央の上下ドラッグで30分単位に変更する |
| 取り消し | 変更前のplan／dailyTimeline／flowOverridesを1世代保存し、「変更を取り消す」で復元する |
| 合格条件 | 未選択タップで変更なし、選択後の3モード、詳細編集、戻す、位置保持、PC／390px実表示、コンソールを確認 |

### DS-FLOW-014 実装前確認

  - [x] 未選択カードのタップが選択だけで終わり、操作モードが表示される。
  - [x] 選択後の予定全体／開始／終了の各モードを表示し、30分単位の変更計算経路へ接続した。
  - [x] 詳細保存後に「変更を取り消す」が表示され、1回で消費されることを確認した。
  - [x] PC／390pxの実表示とコンソールを確認した。共通スモーク検査も通過。

### DS-FLOW-014 直感操作への具体化

  - [x] 操作パネル方式を採用せず、選択中カード自身に操作状態を持たせる方針へ変更した。
  - [x] 選択時だけ上下のハンドルをカード外側へ表示する。
  - [x] 中央の長押しで全体移動、上端ハンドルで開始、下端ハンドルで終了を変更する経路へ接続した。
  - [x] 予定外タップで選択解除し、PC／390pxの実表示とコンソールを確認した。共通スモーク検査も通過。
    - 390px実表示で選択カードに上下2個の48×13pxハンドルがカード外側へ出現し、操作パネルは表示されないことを確認。

### DS-FLOW-014 再発診断（0.31.25）

  - [x] PCの同方向連続移動は再現せず、2回とも30分移動できることを確認した。
  - [x] タッチ中央長押しの開始タイマーが欠落していたことを原因として記録した。
  - [x] タッチ中央は430ms長押し後に移動を開始し、移動後の選択時刻を更新する。
  - [x] 390pxで上下ハンドルが48×13pxで表示され、横はみ出しなし・コンソールエラーなしを確認した。PC実表示で同方向に30分ずつ2回移動できた。

### DS-FLOW-014 ドラッグ後クリックの分離

### DS-FLOW-014 0.31.28 再発診断と合格条件

### DS-FLOW-014 0.31.30 編集導線とつまみ基準

### DS-FLOW-014 0.31.31 編集ボタンのタッチ安定性

### DS-FLOW-014 0.31.33 端末権限と時間割操作

- [x] PC読み取り専用では、予定カードの表示・選択のみを許可し、「予定を編集」・上下ハンドルを表示しない。
- [x] 読み取り専用ではpointerdown/pointerupの両方で変更処理へ進まない。途中で権限が変わった場合も保存しない。
- [ ] iPhone書き込み可能では、編集ボタン・上下ハンドル・予定移動・詳細保存が動作することを確認する。
- [x] PC読み取り専用では、選択・画面位置保持・コンソールエラーを確認した。0.31.33実表示で選択は可能、編集ボタン・つまみ・変更取消は非表示、ドラッグ後も10:00 - 10:30のまま、コンソールエラーなし。

- [x] clickだけに依存せず、編集button自身のpointerupで詳細を開き、直後の合成clickを1回だけ消費する経路を追加した。

- [x] 原因を、カードbutton内に操作用spanを置いた入れ子の操作要素によるSafari／タッチ時のイベント再ターゲットと記録した。
- [x] カード本体を選択・ドラッグ用の操作コンテナにし、「予定を編集」を独立した実buttonとして分離した。
- [ ] PC/390px実表示で、選択後の編集ボタンをタップ1回で詳細が開くこと、カード本体の移動・端調整と干渉しないこと、コンソールエラーがないことを確認する。

- [x] カード本体のタップは選択だけで、選択中もカード本体の再タップでは詳細を開かない。
- [x] 選択中の1時間カードに「予定を編集」が表示され、そこから詳細編集を開ける。
- [x] 移動・開始端・終了端のドラッグ後に詳細が開かず、操作後の確認用変更は「変更を取り消す」で1回復元できる。
- [x] 上下つまみの中心がカードの上辺・下辺の枠線中心と一致する。PC実表示で、カード高さ50pxの状態を確認した。
- [ ] 390px実表示で、編集文言の収まり、カード本体の移動、上下つまみ、詳細編集、取り消し、スクロール位置を確認する。

- [x] 時間制限ではなく、ドラッグ完了後の次の1回のclickを消費する状態で合成clickを詳細表示へ渡さない。
- [x] 合成clickが発火しない端末では、次のpointerdownで抑止状態を解除し、通常タップを犠牲にしない。
- [x] 開始端は終了時刻の30分前まで、終了端は開始時刻の30分後から変更でき、開始・終了の拡大と縮小を同じ境界で扱う。
- [x] PC実表示で、移動後の詳細なし、通常タップの詳細あり、終了端08:30-12:00→11:00、開始端08:30-12:00→09:30を確認し、確認後は元の予定へ戻した。コンソールエラーなし。
- [ ] 390px実表示で、同じドラッグ後の詳細なし、通常タップの詳細あり、開始端・終了端の両方向変更、選択・スクロール位置保持を確認する。

  - [x] ドラッグ完了後の合成clickを1回だけ消費し、詳細を開かない。
  - [x] 通常タップでは詳細が開き、外側タップでは選択が解除される。
  - [x] 実ブラウザで予定全体のドラッグ後に詳細なし、通常タップ後に詳細ありを確認。PC／390pxの表示・コンソールも確認済み。

### DS-FLOW-VIEW-003 週／月カレンダー再設計（0.31.22）

| 項目 | 内容 |
| --- | --- |
| 対象画面 | 一日の流れから開く週／月カレンダー、生活側チェックリスト導線 |
| 目的 | 週は予定の重なり・負荷・毎日の習慣を調整し、月は支払い・通院・締切・習慣の配置を先に見通す |
| 影響度 | 中。表示だけの変更だが、予定を見落とすと生活調整を誤る |
| 可逆性 / 失うもの | 表示構造と導線のみを変更し、予定・習慣・チェックリストのデータは変更しない |
| 初期状態 | 現在日を含む期間を表示し、絞り込みは「すべて」。週／月の目的説明を表示する |
| 入口 | 一日の流れ → 週／月を見る。チェックリストは生活側の既存入口から開く |
| 実行 | 前後期間移動・今日へ戻る・週／月切替・仕事／生活／共通絞り込み・日付選択を行う |
| 中止 / 確認 | 表示切替・移動・絞り込みはデータを変更しない。日付タップだけが同日時間割へ移動する |
| 合格条件 | PC／390pxで週の日別予定名・時刻・毎日予定、月の日付ごとの予定数・色印・次の予定が読め、全操作・同日遷移・位置保持・コンソールを確認する |

### 原因と再発防止

- 原因は、日付点だけを描画して期間移動・日別の内容・毎日予定の識別を提供していなかったこと、および時間割に生活側と重複するチェックリスト導線が残っていたこと。
- 週／月とも同じ期間キーと `allBlocks` を使い、週は一覧、月は見通しという役割だけを変える。毎日予定は表示元を保持して「毎日」と示す。
- UI変更後は、週→月→前後移動→今日→絞り込み→日付タップをPC／390pxで実操作し、データ不変・横はみ出しなし・コンソールエラーなしを確認する。

### 実表示確認結果（0.31.22実装後）

- [x] 時間割からチェックリストボタンが消え、生活側の「生活の習慣・やること」入口が維持されている。
- [x] 週表示で7日分の日別予定・時刻が表示され、毎日予定の表示元を表示できる構造になっている。月表示で日付ごとの予定数・色印・次の予定が表示された。
- [x] 前後移動・今日へ戻る・仕事／生活／共通の3種の絞り込み・日付タップが動作した。390pxで幅375px、横はみ出しなし。PC幅でも表示と通し検査を確認した。
- [x] 日付タップで同じ日付の時間割（2026-08-13）が開き、予定データを変更せずに遷移した。
- [x] `tools/check.ps1` が版・PWA資産・画面遷移・コンソールエラーなしを含めて通過した。
