# くらしのしるし｜設計判断ログ

この文書は、利用中のフィードバック、失敗、提案、修正判断を残すための記録です。
同じ指摘を繰り返さないため、各項目に「原因」「決定」「再発防止」を必ず記します。

> このログは時系列の受付簿です。判断が共通ルールまたは画面仕様として定着したら、`README.md` にある該当の正本にも反映します。

## 運用ルール

1. 利用者から提案・違和感・修正依頼を受けたら、実装前にこのログへ記録する。
2. 見た目の調整でも、先に情報構造・操作の影響度・誤操作時の損失を確認する。
3. 修正後は、実際に表示される画面で確認し、決定した共通ルールに反映されているかを確認する。
4. 再発した場合は、以前の対策が不足した理由も追記する。単に数値だけを変更して終わらせない。

## 2026-08-12｜記録先を先に探索し、重複する箱を作らない

### 利用者提案

修正や提案を記録するときは、他の文書も確認する。すでに同じ目的の記録場所があるのに新しい箱を作り、後から整理・統合する手間を発生させない。

### 原因

設計資料は、判断ログ、操作設計票、共通規則、画面仕様、外部手順、履歴メモに役割が分かれている。記録前の探索を手順として明文化していないと、同じテーマの別ファイルや別セクションを作り、現行正本と履歴が分散する。

### 決定・再発防止

1. 記録前に `design/`、`docs/`、必要な `private/` をキーワード、画面名、操作名、設計ID、版で検索する。
2. 既存の記録を「現行正本」「判断ログ」「操作設計票」「外部手順」「履歴」に分類する。
3. 同じ目的の箱がある場合は新規ファイル・新規IDを作らず、既存の箱へ追記する。判断は判断ログ、操作の合格条件は既存の操作設計票、共通規則は共通正本へ反映する。
4. 別の箱が必要なのは、対象・責務・保存期間・公開範囲のいずれかが明確に異なる場合だけとする。その場合も、入口の索引と関連文書へのリンクを同時に追加する。
5. 記録後に同じキーワードで再検索し、同じ内容の重複箱が増えていないことを確認する。

### 実施結果

- [x] 既存の `design/README.md`、判断ログ、操作設計票、共通規則、画面仕様、`docs/README.md` を確認した。
- [x] 新しい記録ファイルは作らず、既存の記録先選定ルールへ反映する。
- [x] 今後の記録先と新規箱を作る例外条件を、HANDOFFと設計入口にも反映する。

## 2026-08-12｜現行版・コード・外部資料の整合監査

### 利用者フィードバック

コード全体と外部資料を見直し、版、設計判断、操作設計、公開設定、最新手順、整理状態の整合性を確認して、根幹を丈夫にする。

### 原因

`0.31.3` の実装と公開物は一致していたが、HANDOFFは `0.30.0` のまま、日報が未実装扱い、位置保持の正本に反対の記述、ヘルスケア旧手順の現行導線との混在が残っていた。また、旧UIと新UIが同じ `index.html` / `ui-v2.js` 上に重なり、CSSの `!important` 依存も過去の監査時点より増えていた。

### 決定・再発防止

1. まずHANDOFF、正本、操作設計票、外部手順を現行版へ揃える。
2. 次に描画の共通入口と自動検査を強化し、位置保持・日報・体調分析・公開版を検査対象にする。
3. その後、旧UIと新UIを画面単位で整理し、CSSを共通トークンと部品へ移行する。
4. 各段階でローカル実表示、公開版、最後にSafari実機を確認する。文書だけを更新して合格とはしない。

### 監査時点の合格条件

- `version.txt`、HTML、Service Worker、公開URLの版が一致する。
- 現行手順と旧方式が明示的に分離される。
- 保存・選択・削除・ドラッグ等の位置保持規則が正本間で矛盾しない。
- 自動検査が主要画面と今回の仕事の記録・体調分析を対象にする。
- 旧UI/CSS整理後も既存データを壊さず、実表示でPC・スマホ幅を確認する。

## 2026-08-12｜共通レンダー入口の単一化と位置保持の再確認

### 実装前の判断

監査で、再描画関数に設定表示用のラッパーが重なり、位置保持の引数を将来の修正で落としやすい構造が残っていた。これは日報の保存・体調分析の指標切替・自由ボードの操作で、同じ画面位置保持契約が画面ごとに分岐する原因になる。

### 原因

`newAppRender()` の基本描画と設定画面の表示補正が別ラッパーに分かれていた。さらに、スマホブラウザは日付入力へフォーカスすると、`focusin` より先に入力欄を画面上へ移動する場合があるため、選択前の位置を個別イベントだけで記録すると再発余地があった。

### 決定・再発防止

- `renderV2(options)` を唯一の描画入口とし、通常の再描画は位置を保持、ページ遷移だけ `preserveScroll:false` を明示する。
- 設定画面の同期状態補正を共通入口へ統合し、後段ラッパーを廃止する。検査で `window.newAppRender` が1箇所、旧ラッパーが0箇所であることを確認する。
- 日付入力のブラウザ管理スクロールに備え、短い位置履歴を保持する。ただし実機で入力欄を選べる状態のタップを正本とし、単なるコード文字列検査では合格にしない。
- 日報の実作業時間は `workLogMinutes()` のみを計算正本とし、入力中・保存済みサマリー・体調分析で同じ値を使う。

### 実表示の合格条件と結果

- [x] `tools/check.ps1` で `09:00`・`18:00`・休憩`60`を入力し、「8時間0分」を確認した。
- [x] 体調分析に「実作業時間」「休憩時間」が表示され、390px幅で見えている指標をタップしてもスクロール位置が`120px`のまま保持された。
- [x] Codexのローカル390px幅で日付中央、時刻列中央、日報テンプレートの余白を目視確認し、PC幅でも再確認した。
- [x] 実ブラウザ検査でコンソールエラーなし、主要画面遷移、PWA資産版、単一レンダー入口を確認した。
- [ ] 実機Safari、保存・削除・フリーボードを含む全画面の位置保持は次段階の合格条件として残す。

## 2026-08-12｜時間割と仕事の記録を共通の仕事カタログへ統合

### 利用者提案

「一日の流れ」の仕事の時間割と「仕事の記録」を連動させる。仕事内容と優先順位などで分類できる仕事を時間割から追加し、追加済みの仕事を日報のプロジェクト／作業ドロップダウンから選べるようにする。既存の日報のプロジェクト・作業チェック欄は不要なので廃止する。日報テンプレートは、提示された仕事管理表のように、状況・やること・試行／メモ・納品／成果物・次回を残せる形へ整理する。

### 原因

現状は、時間割が `S.plan[日付]` の自由テキスト、日報が `S.workLogs[日付].project` の自由テキストと `checks` の配列に分かれている。同じ仕事を入力しても同一性を判定できず、優先順位・状態・プロジェクトを再入力する必要があるため、管理が二重になっていた。

### 決定・再発防止

- 既存の設計箱を使い、仕事の管理単位を `S.workProjects`（プロジェクト）と `S.workItems`（仕事内容）へ固定する。仕事にはプロジェクト、優先順位、状態、色、メモを持たせる。
- 時間割の「仕事を追加」は、仕事内容を仕事カタログへ登録すると同時に、その日の時間割へ `workItemId` 付きで配置する。従来の自由入力予定・生活予定は残す。
- 仕事の記録は自由入力を正本にせず、プロジェクトと仕事内容を既存カタログから選ぶ。時間割に登録した開始・終了は候補の予定時間として日報へ提示するが、実績時間は日報側で別に記録する。
- 既存の `S.areas` と古い `project` 文字列は削除せず、初回正規化時にプロジェクト候補・表示値へ移行できるようにする。
- プロジェクト作業チェック欄は削除し、チェックを保存しない。既存の `checks` は読み込み時に破棄せず、互換用データとして残すが現行画面では使わない。
- 仕事の優先順位と状態は、仕事の追加時に設定し、時間割・日報の候補・保存済みサマリーで同じラベルを表示する。状態は未着手／進行中／待ち／完了、優先順位はA〜Dで統一する。

### 実表示での合格条件

- [x] 一日の流れで「仕事を追加」を選び、プロジェクト、仕事内容、優先順位、状態、開始・終了を入力して登録できる。
- [x] 登録した仕事が時間割に表示され、同じ仕事が仕事の記録のプロジェクト／作業ドロップダウンに出る。
- [x] 日報で仕事を選ぶと、時間割の予定開始・終了が候補として表示される。実績の開始・終了は別に変更できる。
- [x] プロジェクト作業チェック欄が表示されず、日報保存データに新しいチェック選択を追加しない。
- [x] 日報テンプレートで状況、やること、試行／メモ、納品／成果物、次回を保存・再表示できる。既存の日報本文は失われない。
- [x] 390px幅とPC幅で、追加フォーム、選択欄、時間割、日報テンプレートが重ならない。保存・選択・追加後も画面位置を保持する。

### 実表示確認結果（2026-08-12）

- [x] ローカル実表示で時間割から仕事を追加し、同じ仕事が日報のプロジェクト／仕事内容ドロップダウンへ現れることを確認した。
- [x] 日報で予定時間を候補表示し、やったこと・状況・やること・試行／メモ・納品／成果物・次回を保存して再表示した。
- [x] 390px幅で横スクロールが発生せず、旧プロジェクト作業チェック欄が表示されないことを確認した。
- [x] 自動スモーク検査で、仕事カタログ追加、連動ドロップダウン、既存画面遷移、コンソールエラーなしを確認した。

---

## 2026-08-12｜仕事の記録：作業時刻の整列と休憩控除後の実作業時間

### 利用者フィードバック

スマホ版で「作業開始」と「作業終了」の時刻表示がずれて見える。休憩時間を作業時間から引いた実作業時間も、その場で見えるようにしたい。スマホ表示で同じ問題を再発させない設計を求められた。

### 原因

作業開始・終了はネイティブの `input[type=time]` に時刻表示を任せており、日付だけを中央表示する補助表示と構造がそろっていなかった。そのため、スマホブラウザの内部部品が時刻を左寄せで描画し、2列のラベル／枠の視覚的な中心と一致しなかった。また、保存後や分析では `workLogMinutes()` が休憩を控除していたが、入力途中の画面には控除後の値を表示する派生表示がなく、利用者が実作業時間を確認できなかった。

### 決定・再発防止

- 日付・作業開始・作業終了は、実入力を維持したまま、同じ中央表示用のラッパーと `number` トークンを使う。入力欄は等幅2列の `grid` で計測し、ラベル・値・枠の中心を同じX軸に置く。
- 実作業時間は「終了−開始−休憩」で算出し、保存済みサマリー、体調分析、入力中の表示で同じ `workLogMinutes()` を使う。計算結果を別保存して不整合を作らない。
- 開始・終了が未入力のときは「未計算」、休憩が作業時間を超えるときはエラー状態を表示し、負の実作業時間を表示しない。
- 390px幅とPC幅の実表示で、2列の各ラベル・値・枠の中心座標、実作業時間の計算、休憩超過の拒否を確認する。単一ブラウザの見た目だけで合格にしない。

### 実表示の合格条件

- [x] 390px幅で作業開始・作業終了の値が各入力枠の中央に揃い、ラベルと値の中心が列中心と一致する。
- [x] 開始09:00、終了18:00、休憩60分で「実作業時間 8時間00分」と表示される。
- [x] 開始・終了未入力は「未計算」、休憩超過はエラー状態になり、保存されない。
- [x] 保存済みサマリーと体調分析の作業指標が同じ控除後計算 `workLogMinutes()` を使うことを確認した。
- [x] 実操作後も画面位置を失わず、ゴシック・数字の縦横比1:1・コンソールエラーなしを確認する。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル確認ページで変更前後を開き、390px幅・PC幅を実表示で確認した。
- [x] 390px幅の各時刻値と入力枠の中心差分は0px、PC幅も中心差分0pxだった。
- [x] 実際に09:00／18:00／休憩60分を入力し、実作業時間8時間0分を確認した。
- [x] 休憩600分では実作業時間を未計算にし、保存を拒否した。コンソールエラーはなかった。

## 2026-08-12｜仕事の記録：日付の中央揃えと日報テンプレートの余白

### 利用者フィードバック

スマホ版の仕事の記録で、日付入力の値が枠内で中央に見えない。また、「実装内容」の入力枠と次の「品質改善」ラベルの間隔が近く感じる。確認版はCodex上でページを開き、実際に操作して確認したい。

### 原因

日付はネイティブの `input[type=date]` 内部部品が表示を担うため、通常の `text-align` だけではブラウザごとの値・カレンダーアイコンの配置を統一できない。テンプレートは各入力を同じ `gap` で積んでいるが、長文入力欄の後に次の項目へ移る余白としては不足している。

### 決定・再発防止

- 日付は入力要素そのものの標準表示に任せず、値の表示を共通トークンで中央揃えできる構造にする。日付選択操作は既存の実入力を維持し、見た目だけを分離しない。
- 日付・時刻・数値は `number` トークンと `grid/flex` の中央揃えを使い、ブラウザ標準の内部部品に依存した位置調整をしない。
- 日報テンプレートは入力欄の下端から次のラベルまでの余白を共通トークン化し、長文欄でもラベルと枠が接近しないようにする。
- Codexの確認ページを変更前後に開き、スマホ幅で実表示・実操作・コンソールを確認する。コード文字列や画像だけで合格にしない。

### 実表示の合格条件

- [x] 390px幅で日付の値が入力枠の水平方向中央に見える。日付選択操作と保存対象の日付は変わらない。
- [x] 「実装内容」の枠と「品質改善」のラベルが視覚的に離れ、入力欄の境界と次の見出しが接触しない。
- [x] Codexで開いたローカル確認ページ上で、日付変更・入力・無効な保存検証を実操作できる。
- [x] 画面位置保持、ゴシック書体、数字の縦横比1:1、PC幅と390px幅、コンソールエラーなしを確認する。

## 2026-08-11｜設定操作は「配置」より先に影響度で整理する

### きっかけ

デザイン設定センターで「設定を保存」「初期設定に戻す」「設定を書き出す」「設定を読み込む」を横並びにし、幅不足で「設定を読み込む」だけが折り返された。

### 根本原因

ボタンを先に同列の要素として扱い、各操作の目的・可逆性・誤操作時の損失を整理しないまま、整列と余白だけで解決しようとした。

### 決定した情報構造

| 操作 | 役割 | 影響度 | 表示上の扱い |
| --- | --- | --- | --- |
| 設定を保存 | 現在の設定を確定する | 低い・日常操作 | 主操作として独立表示 |
| 設定を書き出す | 設定の控えを作る | 低い・安全 | 「設定の入出力」グループ |
| 設定を読み込む | 保存済み設定を反映する | 中程度・上書きの可能性 | 「設定の入出力」グループ、実行前に確認 |
| 初期設定に戻す | 現在の調整を失う | 高い・破壊的 | 保存から離し、詳細操作／初期化として隔離、確認必須 |

### 実装の順序

1. 情報と操作を、役割・影響度・可逆性で分類する。
2. 誤操作時の損失が大きい操作を隔離または段階表示にする。
3. 同じ役割・近い重さの操作だけを並列にする。
4. 最後に、枠・中央揃え・余白・横並び／縦並びを決める。

### 再発防止チェック

- [ ] 先に「何をする操作か」を文章で定義したか。
- [ ] 誤操作で失われるものを確認したか。
- [ ] 破壊的・上書き操作を主操作の隣に置いていないか。
- [ ] 並列配置は、意味と影響度が近い操作に限定されているか。
- [ ] レイアウトは情報構造の決定後に検討したか。

---

## 2026-08-11｜設定センターは操作書体と紙面書体を分離する

### 問題

設定センターの見出しが明朝体になっていた。これは「機能画面はゴシックで統一する」という共通規則と矛盾する。

### 原因

紙面装飾用の `display` 書体トークンを、設定センターの `h1` とプレビュー見出しへ直接適用していた。`display` を「見出し全般」と誤って扱い、用途別の書体分類がコードに反映されていなかった。

### 決定

1. 設定・フォーム・操作・機能画面の見出し・金額は UI ゴシックを使用する。
2. 明朝系は、装飾的なロゴか、利用者がフリーボードで選ぶ表現用途に限定する。
3. `display` トークンを使う場合は、紙面装飾である理由を仕様へ明記する。
4. デザイン設定の変更は即時にプレビューするが、端末への確定保存は `設定を保存` のみで行う。

### 再発防止

- [ ] コンポーネントに書体を指定する前に、`操作 / 情報 / 装飾 / 利用者コンテンツ` のどれかを判定する。
- [ ] 画面実装前に、影響度で操作をグループ化してからレイアウトを決める。
- [ ] 設定センターは保存・入出力・初期化の3群が視覚的に分かれているか、実表示で確認する。

---

## 2026-08-11｜危険操作の「入口」と「実行」を同じ枠にしない

### 再発した問題

初期設定に戻す操作を折りたたみにはしたが、破線の大きな外枠全体が初期化の操作範囲に見えた。危険操作を通常操作と同じ部品として扱ったため、説明を開く行と実行範囲が混同された。

### 根本原因

「折りたたみ」という見た目だけを適用し、入口・説明・実行・中止を別々の役割として設計していなかった。操作の構造より先に枠と文字を配置してしまった。

### 決定

1. 操作を作る前に、目的、影響度、可逆性、失うもの、入口、実行、中止、合格条件を操作設計票へ記録する。
2. 危険操作は、開閉入口と実行ボタンを別部品にする。入口は実行しない。
3. 実行後に戻せない可能性がある操作は、専用ボタンと確認ダイアログの二段階にする。
4. 合格条件を実表示で確認するまで、他ページへ横展開しない。

### 再発防止チェック

- [ ] 操作設計票に入口と実行が別項目としてあるか。
- [ ] 危険操作の外枠全体が実行範囲のように見えないか。
- [ ] 「開く」「実行」「キャンセル」を実画面で試したか。
- [ ] 同じ失敗を配置調整だけで処理していないか。

## 2026-08-12｜日報を独立した「仕事の記録」として追加する

### きっかけ

日報を既存の「一日の流れ」や「こころとからだ」の入力欄へ混ぜず、仕事の記録として独立して残し、作業時間・休憩時間を体調分析で比較したいという提案を受けた。

### 根本原因

現行画面には予定・習慣・体調の記録はあるが、仕事の開始／終了、休憩、作業内容を一つの日付単位で保存するデータ構造と入口がない。そのため、予定の合計を作業時間と誤認したり、休憩を体調分析へ渡せない。

### 決定した情報構造

| 操作 | 役割 | 影響度 | 表示上の扱い |
| --- | --- | --- | --- |
| 仕事の記録を開く | 独立した日報画面への入口 | 低い | 「今日を整える」内の独立した主入口。ホームのショートカット候補にも登録 |
| 日報を保存 | 作業・休憩・本文を日付単位で確定 | 中 | 仕事の記録画面の主操作として一つだけ表示 |
| 保存済み日報を見る | 当日の記録と集計を確認 | 低い | 保存後のサマリーとして同じ画面に表示 |
| 体調分析で作業／休憩を表示 | 保存済みデータを比較材料へ渡す | 低い | 既存の分析指標切替へ追加。未記録日は欠測として扱う |

### 保存モデル

`S.workLogs[YYYY-MM-DD]` に `start`、`end`、`breakMinutes`、`project`、`checks`、`implementation`、`quality`、`design`、`insight`、`next` を保存する。作業時間は開始・終了から算出し、休憩時間は入力値を分単位で保持する。体調分析はこの保存モデルだけを読み、予定の表示時間を代用しない。

### 再発防止

- 日報は `flow` の予定編集と別ルート・別データとして扱う。
- 保存後は入力欄を空にし、保存済みサマリーを画面に残す。
- 保存・選択・削除・ドラッグに該当する再描画では現在の画面位置を保持する。ページ遷移だけ先頭へ戻す。
- 作業時間・休憩時間は `number` トークンと操作用ゴシックで表示し、文字の縦横比を変えない。
- 分析では未記録日を0分として数えず、平均・グラフを欠測のまま扱う。

### 実表示での合格条件

- [ ] 「今日を整える」から、予定とは別の「仕事の記録」を開ける。
- [ ] 作業開始・終了、休憩分、プロジェクト、本文テンプレートを入力して保存できる。
- [ ] 保存後に入力欄が空になり、保存済みの作業時間・休憩時間・本文の要約が同じ画面に残る。
- [ ] 保存後にページ先頭へ戻らず、長い画面の同じ位置を保持する。
- [ ] 体調分析で「作業時間」「休憩時間」を個別に表示切替でき、未記録日は線や棒を捏造しない。
- [ ] PC幅と390px前後のスマホ幅で、操作画面がゴシック、数字が1:1、入力とボタンが重ならない。

---

## 2026-08-11｜合格条件はCSSの実表示まで確認する

### 実測で見つかった問題

初期設定に戻すを `details` で閉じても、パネル側の `display: grid` がブラウザ標準の非表示規則を上書きし、実行ボタンが残っていた。

### 決定

1. 開閉UIはHTML構造だけで合格とせず、閉じた状態・開いた状態を実ブラウザで測定する。
2. 初期状態では実行部品を `display: none` にし、`[open]` の時だけ表示する。
3. 危険操作の合格条件には「実行ボタンの可視性」を必ず含める。

### 実表示の確認結果

- 閉じた状態: 実行ボタンは非表示、入口の高さは44px。
- 開いた状態: 説明と専用実行ボタンだけが表示される。

---

## 2026-08-12｜導線を仕事・生活・見える化へ分け、利用実績を確認できるようにする

### 利用者フィードバック

- 実際に使っているのはショートカットの「支出・収入を記録」「一日の流れ」「こころとからだ」で、ホームの「記録する」「今日を整える」は役割が抽象的で使っていない。
- 「見える化する」は分析としてまとまっているため、入口の意味が分かりやすい。
- 仕事と生活は別グループとして扱い、共通化するものは後から判断したい。ページを何回開いたかなど、利用状況を見て導線を改善できるとよい。

### 原因

ホームは「記録する」「今日を整える」「見える化する」という動詞の分類だが、実際の利用目的は「お金」「仕事の時間割」「こころとからだ」「分析」という領域分類だった。「記録する」にはお金と体調、「今日を整える」には仕事と生活が混在しているため、利用者が目的から入口を推測しなければならない。ショートカットは具体的な行動名なので、抽象的なホームカードより直接使われている。

### 暫定判断・再発防止

1. 次の導線案では、トップレベルを「仕事」「生活」「見える化」に分ける。「見える化」は現行の理解しやすさを維持する。
2. 「仕事」には仕事の時間割・仕事の記録・仕事カタログを置き、「生活」には支出・収入、こころとからだ、生活の予定・習慣を置く。仕事と生活の混在した「一日の流れ」は、分離後の役割を実表示で再確認してから扱う。
3. 共通の時間軸・共通カタログ・共通トップ入口は、仕事／生活の役割が分かれた後に、重複が実測で確認できたものだけ再統合する。
4. 利用履歴は外部送信しない端末内の任意機能とし、ページ遷移とショートカット選択を画面ID・入口・日時単位で記録する。再描画や同一画面内の操作をページ利用回数に数えない。
5. 履歴は設定または見える化の補助画面で確認・消去でき、保持期間を限定する。記録を無効にしても本体の記録・同期・ショートカットは壊さない。

### 実装前に確認する合格条件

- [x] ホームで「仕事」「生活」「見える化」の役割が説明なしに判別できる。
- [x] 仕事の入口から仕事の時間割・仕事の記録へ、生活の入口から生活の記録へ迷わず到達できる。
- [x] 既存ショートカットの3入口は、再設計後も1タップで同じ目的へ到達できる。
- [x] 「見える化する」は分析入口として現在と同等以上に分かりやすい。
- [ ] 利用履歴は端末内だけに保存され、ページ遷移とショートカット利用を区別して集計できる。
- [ ] 利用履歴の無効化・消去ができ、履歴追加や画面遷移で入力位置を失わない。
- [x] 390px幅とPC幅で、仕事・生活・見える化のグループ、ショートカット、履歴表示が重ならない。

### 実表示確認結果（2026-08-12）

- [x] ホームを3グループへ再構成し、仕事の時間割／仕事の記録、支出・収入／こころとからだ／生活の習慣・やること、お金／体調の分析を目的別に表示した。
- [x] 390px幅でグループ・ボタン・ショートカットに横はみ出しがなく、実表示の遷移で各入口を確認した。
- [x] 既存ショートカット（お金の分析・体調の分析・こよみ）が残り、直接遷移できることを確認した。
- [x] 利用履歴は今回実装していないため、履歴の保存・集計・消去条件は次段階へ保留した。

---

## 2026-08-12｜時間割の仕事・生活・共通分類と優先度別の次の行動

### 利用者フィードバック

- 時間割を仕事枠で見ているときは、生活の予定を混ぜず仕事だけを表示したい。
- 仕事枠の「予定を足す」は、仕事を登録する操作なので「仕事内容を追加する」と表示したい。
- 仕事と生活が同じ時間割にある感覚は残したい。仕事にも生活にも当てはまるものを無理にどちらかへ寄せず、「共通」の枠を検討したい。
- 画像のように「次にやる」「今すぐやる」「いつかやる・たぶんやる」「待ち」で分類し、優先順位をつけて、次に何をすべきか一目で分かるようにしたい。
- 「未着手」「進行中」「待ち」「完了」は状態であり、優先度グループとは別に扱う。

### 原因

現行の時間割は仕事と生活を左右のレーンへ同時表示し、追加フォームも「予定」「仕事」「毎日やること」を同じ入口で選ばせている。仕事カタログの優先順位はA〜Dの抽象値で、画像のような行動順の分類と一致していないため、登録後に次の仕事を判断する一覧性が不足している。また、仕事・生活のどちらか一方に決めにくい予定を受け止める分類がない。

### 決定・再発防止

1. 時間割は共通の一つの時間軸として残し、表示切替を「すべて」「仕事」「生活」「共通」にする。「すべて」は従来どおり全体を見渡すための入口とする。
2. 仕事枠では `flowLane(block) === "work"` の項目だけを表示し、主操作を「仕事内容を追加する」とする。生活枠では生活項目だけを表示し、従来の予定追加を使う。共通枠はどちらにも分類しにくい予定を明示的に置く。
3. 「共通」は何でも入れる曖昧な箱にせず、仕事・生活の両方に関係する予定だけに使う。共通項目を仕事・生活の両方へ複製表示せず、「すべて」または「共通」で確認する。
4. 仕事の優先度は `next`（次にやる）、`now`（今すぐやる）、`someday`（いつかやる・たぶんやる）、`waiting`（待ち）の4値を正本にする。既存のA〜Dは互換値として保持し、初回正規化で新分類へ移行する。
5. 仕事一覧は優先度グループ順に表示し、各行に仕事内容・プロジェクト・状態を示す。各仕事の優先度は一覧から変更できる。削除は今回の主操作へ追加しない。
6. 優先度と状態を同じ色・同じ項目として扱わない。優先度は次の行動順、状態は進捗状態として別の選択肢にする。

### 実装前に確認する合格条件

- [x] 「すべて」「仕事」「生活」「共通」の切替で、選択中の分類だけを表示できる。
- [x] 仕事枠では仕事の時間割だけが表示され、生活項目が混ざらない。
- [x] 仕事枠の主操作が「仕事内容を追加する」と表示され、仕事カタログへ登録できる。
- [x] 生活枠では生活項目だけが表示され、従来の生活予定を追加できる。
- [x] 共通枠には仕事・生活の両方に関係する項目だけを置け、すべての表示では共通項目も確認できる。
- [x] 仕事一覧が「次にやる」「今すぐやる」「いつかやる・たぶんやる」「待ち」の順で表示される。
- [x] 仕事一覧から優先度を変更すると、該当グループへ移動し、保存後も再表示される。
- [x] 390px幅とPC幅で、グループ、優先度選択、追加フォーム、時間割が重ならない。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル実表示で、PC幅と390px幅の時間割を開き、仕事枠・生活枠・共通枠の表示切替を確認した。
- [x] 仕事枠の主操作が「仕事内容を追加する」となり、仕事追加フォームの優先度が4分類で表示されることを確認した。
- [x] 共通枠の追加フォームで、分類が「共通」に初期選択されることを確認した。保存操作は行わず、利用者データを変更していない。
- [x] 仕事一覧で4つの優先度グループと状態表示を確認した。
- [x] `tools/check.ps1` の画面遷移・仕事カタログ・日報連携スモーク検査がコンソールエラーなしで完了した。

### 次段階の整理順序（追加記録）

- 「時間を決めて追加」の選択肢・ボタン文言・設定項目は、上記の分類と優先度の実装・実表示確認が完了した後に横断整理する。
- 整理では、同じ意味の入口を増やさず、仕事・生活・共通の分類と矛盾しない選択肢だけを残す。
- ボタン文言は、押した結果が分かる動詞に統一し、設定は追加操作に必須なものと任意のものを分離する。
- 整理後の合格条件は、PC幅・390px幅の実表示で選択肢、主操作、設定の関係が一度で理解でき、同じ予定を複数入口から重複登録しないこととする。

---

## 2026-08-12｜既存予定の編集項目と毎日予定の全日付削除、同時進行表示

### 利用者フィードバック

- 既存予定の編集画面にも「優先順位」「状態」を追加し、あとから編集できるようにしたい。
- すべての日程に入っている3つの毎日予定は時間割から削除したい。
- 予定が同じ時間帯に重なっても、最大3件程度を同時に見えるようにしたい。

### 原因

既存予定の編集シートは名前・開始・終了・区分だけを扱い、追加時に持たせた優先順位・状態を後から変更できなかった。また、毎日予定の削除が日付ごとの上書き削除になっており、全日付から消したい場合に同じ予定が残り続ける。時間割イベントは同じ列の全幅へ描画していたため、同時間帯の予定が重なると後の予定を隠していた。

### 決定・再発防止

1. 仕事の既存予定の編集シートに、仕事一覧と同じ4段階の優先順位、4種類の状態を追加する。仕事カタログに紐づく予定はカタログ側も同時更新し、自由入力の仕事予定は予定データへ保存する。
2. `dailyTimeline` の予定を編集して削除する場合は「この日の表示だけ」ではなく、毎日予定の正本を削除し、全日付から消えることを明示する。自動予定は削除対象にしない。
3. 時間帯の重なりを共通レイアウト計算で列分けし、最大3件の同時進行を横並びで表示する。列数・列位置はイベントごとの重なりグループから算出し、非重複の予定は従来どおり広く表示する。
4. 予定の保存・削除後も共通レンダーの画面位置保持契約を使い、PC・390px幅の実表示で既存予定、毎日予定の全日付削除、3件重複を確認する。

### 実装前に確認する合格条件

- [ ] 既存の仕事予定を開くと優先順位・状態が表示され、変更して保存できる。
- [ ] 仕事カタログに紐づく予定の編集が、時間割・仕事一覧・日報候補の同じ仕事情報と矛盾しない。
- [ ] 毎日予定の削除は全日付から消えることを明示し、削除後に別の日付にも表示されない。
- [ ] 同時間帯に3件の予定を置くと、3件すべての名前・時刻が見える。
- [ ] 390px幅とPC幅で編集シート、削除確認、重複列が重ならず、保存・削除後も画面位置を失わない。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル実表示で既存の「生活管理アプリ」を開き、優先順位・状態の選択欄が編集シートに表示されることを確認した。
- [x] 390px幅の実表示で編集シートを開き、既存の名前・開始・終了・区分と追加項目が重ならないことを確認した。
- [x] `tools/check.ps1` の実ブラウザ検査で、既存仕事予定の編集項目、同時間帯3件の3列レイアウト、毎日予定の「毎日の予定から削除」ラベルを確認した。
- [ ] 利用者のiPhone正本にある3件の実データ削除は、PC読み取り専用端末からは実行していない。iPhone側で毎日予定を削除し、同期後に別日にも残らないことを確認する。

## 2026-08-12｜時間割の分類統合・枠内表示・予定編集位置

### 利用者フィードバック

- 仕事表示では時間割の区切りを「仕事」だけ、生活表示では「生活」だけにする。
- 「すべて」と「共通」を統合し、「共通」はすべての予定を表示する。
- 基本予定は時間割の枠からはみ出さず、予定タップ後の編集画面は画面中央に出す。
- 編集画面の開始・終了の位置ずれをなくし、基本時間の表示を再発させない。

### 原因

時間割の表示は、分類フィルターの意味と列見出しの構造が分離されておらず、「仕事／生活」の2列を常に描画していた。また、予定カードの位置・幅に旧CSSの列指定が残り、スマホ幅ではカードの幅計算と枠の幅が競合して右側へはみ出す経路があった。編集画面は下端寄せのシートで、時間入力は通常のフォーム行へ直接置かれていたため、端末幅やブラウザの標準入力スタイルによって開始・終了の見た目がずれた。

### 決定・再発防止

1. 時間割の表示モードは「仕事」「生活」「共通」の3つに統一する。「共通」は分類名ではなく、すべての予定を表示するモードとする。
2. 「仕事」「生活」では選択中の1区分だけを時間割見出しとして描画し、反対側の見出しと中央区切りを出さない。「共通」だけは仕事・生活の両方を見出しとして表示する。
3. 予定カードは時間割コンテナを基準に、重なりレイアウトの左位置と幅を計算する。`box-sizing`、最大幅、長い文字の折り返しを共通トークン側で固定し、旧区分CSSが計算結果を上書きしないよう表示モード属性を持たせる。
4. 編集シートは画面中央配置を共通レイヤーで定義する。開始・終了は同じクラスの2列グリッド、幅100%、`tabular-nums` で描画し、ブラウザ固有の入力幅に依存しない。
5. 保存・選択・削除・表示切替は現在位置を保持し、戻る・ページ遷移だけ先頭へ戻す。PC幅と390px幅の実表示で、3モード、枠内、中央シート、開始・終了の揃いを確認する。

### 実装前に確認する合格条件

- [x] 仕事表示の時間割見出しが仕事だけで、生活表示が生活だけになる。
- [x] 共通表示に切り替えると、仕事・生活・共通の全予定が表示される。
- [x] 基本予定のカードがPC幅・390px幅で時間割枠を横にはみ出さない。
- [x] 予定タップ後の編集シートが画面中央にあり、開始・終了のラベルと入力欄が同じ列幅で揃う。
- [x] 表示切替・保存・削除後も画面位置と既存データを失わない。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル実表示で仕事・生活・共通を順に開き、仕事表示は仕事のみ、生活表示は生活のみ、共通表示は両方の見出しになることを確認した。
- [x] PC幅と390px相当の実表示で、時間割カードの左右端が時間割シェル内に収まることを実測した。
- [x] 編集シートをbody直下の共通モーダルとして中央配置し、開始13:30・終了16:00の入力欄と優先順位・状態を確認した。閉じる操作も確認した。
- [x] 同一版の旧キャッシュを再利用しないため、`0.31.8` へ版を上げ、HTML・Service Worker・資産クエリを一致させた。

## 2026-08-12｜月次のお金分析と全財産の内訳表示

### 利用者フィードバック

- お金の分析は一か月ごとに見たい。
- 全財産を、借入・現金・口座に分けて見たい。
- 金額は一括で隠せるようにしたい。

### 原因

お金の分析期間が「今日から29日前まで」という移動する30日間で、暦月の支出・収入を比較できなかった。また、見通しは借入合計と残り金額を示すだけで、口座内訳・現金・借入を同じ表示ルールで確認するまとまりがなかった。金額の表示切替も個別の値に依存すると、内訳追加時に一部だけ見える再発余地がある。

### 決定・再発防止

1. 分析対象は選択中の暦月（YYYY-MM）に固定し、前月・次月の移動を同じ月境界関数で行う。30日減算や端末時刻による月跨ぎ判定は使わない。
2. 全財産の内訳は、借入合計、現金・手元、口座ごとの残高を同じセクションにまとめ、借入は返済義務があることをラベルで明示する。借入を収入分析へ混ぜない既存判断は維持する。
3. 内訳の金額は同じ「お金を表示／隠す」状態に接続し、表示切替で借入・現金・全口座の金額が同時に切り替わる。ラベルと合計の意味は隠さない。
4. 390px幅・PC幅で月移動、月見出し、内訳、表示切替を実表示確認し、分析月を変更しても入力中の記録や画面位置を失わない。

### 実装前に確認する合格条件

- [x] お金の分析に暦月の見出しと前月・次月操作があり、月境界をまたいでも正しい月が表示される。
- [x] 借入・現金・口座ごとの残高が分かれて表示され、借入が収入合計や分析へ混ざらない。
- [x] 金額の表示切替で内訳と合計が一括して隠れ、再表示で同じ値へ戻る。
- [x] PC幅・390px幅で内訳が横にはみ出さず、コンソールエラーがない。

### 実表示確認結果（2026-08-12）

- [x] Codexのローカル実表示で「2026年8月」を開き、前月操作で「2026年7月」へ移動することを確認した。
- [x] 「全財産（借入を含む）」「借入合計」「現金・手元合計」「口座合計」と口座名ごとの残高を確認した。
- [x] 「隠す」で内訳を含む11件の金額が一括して `---` になり、借入・口座名のラベルが残ることを確認した。

## 2026-08-12｜iPhoneを正本にしたPC読み取り専用共有

### 利用者フィードバック

iPhoneのデータを壊さずにPCへ共有し、PCは読み取り専用にしたい。画像はiPhone側の同期設定であり、PCがその既存の同期先へ参加する順番と、PCから書き戻さないことを画面で明確にしたい。

### 原因

既存の同期処理は、Gist IDを指定して参加するとPC側のローカルデータを既存Gistの内容へ置き換える安全確認を行う一方、現行の設定UIには端末の役割選択がなく、旧UIの役割状態を引き継げない経路があった。さらに、同期開始時に設定値を丸ごと置き換えていたため、読み取り専用を先に選んでも開始処理で役割が失われる余地があった。結果として、iPhoneを正本にする意図、PC側だけが置き換わること、PCからの自動送信が止まることが画面だけでは保証できなかった。

### 決定・再発防止

1. iPhoneを正本、PCを読み取り専用の受信端末とする場合、PCの初期役割は「読み取り専用（受信のみ）」にする。読み取り専用では既存Gist IDを必須とし、空欄から新しい同期先を作らない。
2. PC側の順序を固定する。①iPhoneの既存同期先を確認、②PC側の記録を同期前バックアップへ退避、③PCでトークンと既存Gist IDを直接入力、④読み取り専用を保存、⑤既存内容に合わせる確認を行う、⑥Gistを受信、⑦接続状態と画面反映を確認する。トークン・Gist IDは文書・チャット・ログへ再掲しない。
3. 受信処理はGistを読み取るだけで、iPhone側のGistをPATCHしない。PC側のローカルデータは受信前に `mainichi.before-sync` へ退避するため、置き換わるのはPC側だけである。
4. 読み取り専用のPCでは手動保存・自動保存からの送信を止め、同期状態に「読み取り専用」を含める。記録・送信できる端末へ戻す操作は同期設定内の補助操作として分離し、確認を必須とする。
5. 端末データ同期とヘルスケア自動取り込みは、従来どおり別Gist・別設定として扱う。受信後にヘルスケアを扱う場合も、端末データ同期 → ヘルスケア受信の直列順序を崩さない。

### 実表示での合格条件

- [ ] 設定画面でPCの役割が「読み取り専用（受信のみ）」として明示され、未設定のPCでは安全側が初期選択される。
- [ ] 読み取り専用で既存Gist IDを空欄のまま開始すると、新規Gistを作らず中止する。
- [ ] 既存Gistを指定した開始確認で、PC側のデータだけが置き換わること、iPhone側へ書き戻さないこと、同期前バックアップがあることが分かる。
- [ ] 読み取り専用の接続後、PCの自動保存・手動保存がGistへ送信せず、状態表示が「読み取り専用・接続確認済み」になる。
- [ ] PC幅・390px幅で役割、既存Gist入力、受信操作、注意文が重ならず、保存・選択後に画面位置を失わない。
- [ ] 実表示でコンソールエラーがなく、端末データ同期とヘルスケア受信の設定・状態が混ざらない。

### 実表示確認結果（2026-08-12）

- [x] 未設定のPCで「読み取り専用（受信のみ）」が初期選択され、状態が「読み取り専用・未設定」と表示された。
- [x] トークンだけを入力して開始した場合、既存Gist IDを要求して中止し、新規同期先を作らないことを確認した。
- [x] 役割保存後も端末データ同期の詳細を開いたまま、現在の画面位置を保持した。
- [x] 390px幅とPC幅で、役割、説明、入力欄、操作ボタンが重ならず、横スクロールが発生しなかった。
- [x] 実表示のコンソールエラーなし。実データへの同期・Gistへの送信は行わず、トークンやGist IDも記録・再掲していない。
- [ ] iPhoneの実Gistを用いた受信確認は、利用者がPC画面へトークンとGist IDを直接入力した後に実施する。値をチャットへ送らない。

---

## 2026-08-12｜母からの借入を収入と分けて残高へ反映

### 利用者フィードバック

傷病手当金が遅れるため、母から一時的に20万円を借りた。借りたお金を見えるようにし、当面は借入金を含めた「今残っているお金」として計算したい。

### 原因

現行モデルは、口座残高、予定収入、実際の収入、支出、カード・固定費を扱うが、返済義務のある借入を保存する専用項目がない。そのため、借入を収入として記録すると収入分析・傷病手当の見込み・返済対象が混ざり、後から借入だけを確認できない。一方、PCは読み取り専用端末のため、PCから借入を直接保存すると同期の書き込み禁止と矛盾する。

### 決定・再発防止

1. 借入は `S.borrowings` に独立保存し、収入ログ・予定収入・傷病手当の見込みへ追加しない。借入先、金額、日付、時刻を保持し、返済記録は別の次段階として扱う。
2. 「今残っているお金」は、口座残高 ＋ 未返済の借入合計 − 未払い固定費 − 今月のカード利用として計算する。借入を含むことを見出しと補助行で明示する。「今日使えるお金」や収入分析へは、今回の要望を超えて自動加算しない。
3. 借入の記録入口は支出・収入と同じ金銭記録画面内に置くが、支出・収入と別タブ・別ラベルにする。返済義務を隠さず、借入を収入として誤登録しない。
4. iPhoneが記録・送信端末、PCが読み取り専用端末の場合、借入の実登録はiPhoneで行い、PCは同期受信で表示する。PCの読み取り専用・既存Gist受信の安全条件を変更しない。
5. 同期前後で `borrowings` を失わないよう正規化・同期ペイロード・バックアップの対象に含め、保存・選択後の画面位置を保持する。

### 実表示での合格条件

- [ ] iPhoneの金銭記録画面で「借入」を選び、借入先「母」、200,000円を保存できる。
- [ ] 借入が収入合計・傷病手当の予定収入へ混ざらず、借入として一覧・見通しに表示される。
- [ ] 「今残っているお金（借入を含む）」が、既存の残高に借入合計を加えて計算される。
- [x] PCの読み取り専用画面では借入を編集・保存できず、「見るだけの端末です」と表示されることを確認した。
- [x] 390px幅とPC幅で、借入タブ、入力、借入表示、見通しの補助行が重ならず、操作後の画面位置を保持することを確認した。
- [x] 既存の支出・収入・傷病手当データを変更せず、実表示でコンソールエラーがないことを確認した。

### 実表示確認結果（2026-08-12・実装後）

- [x] Codexのローカル実表示で、支出・収入画面に独立した「借入」タブ、借入金額、借入先、借入合計を確認した。
- [x] PC読み取り専用端末で200,000円・母を入力して保存操作を行い、既存データを変更せず保存を拒否することを確認した。
- [x] 390px幅で借入入力画面を実表示し、注意書き・保存ボタン・既存記録が重ならず、コンソールエラーなしを確認した。
- [ ] iPhone側で実際に200,000円を保存し、同期後のPCで借入合計と現在残高への反映を確認する。PC側からは登録しない。
## 2026-08-12｜公開版で同じ版番号の資産キャッシュが残った問題

### 再発した問題

借入機能を追加してプッシュしたが、公開URLは `0.31.3` の表示版になった一方、同じ版番号の `ui-v2.js` がService Worker／CDNキャッシュから配信され、公開画面では借入タブが旧表示のままだった。

### 原因

コードを変更したのに `version.txt`、HTML、Service Worker、資産クエリの版を `0.31.3` のまま再利用したため、公開先が新しいHTMLを返しても、ブラウザが同じURLの旧JavaScriptを再利用できる状態だった。版表示の一致だけでは、資産の実体が新しいことを保証できなかった。

### 決定・再発防止

1. 実装変更を公開するときは版を上げ、`version.txt`、`BUILD`、HTMLのCSS/JSクエリ、Service Workerのキャッシュ名・資産クエリを同じ版へ更新する。
2. 公開後は版表示だけで合格にせず、公開URLで変更対象の操作画面を開き、変更固有の要素（今回は「借入」タブ）を実表示で確認する。
3. 公開確認で旧表示が残った場合は、同じ版のまま再試行せず、原因を記録して資産版を切り替える。

### 実表示確認結果（2026-08-12）

- [x] `0.31.4` へ版を上げ、HTML・Service Worker・資産クエリを一致させた。
- [x] 公開URLで `0.31.3` の旧キャッシュ配信を検出した。`0.31.4` の再公開後に借入タブを再確認する。

# 2026-08-11: 記録訂正・モバイル習慣・買い物削除

## 利用者フィードバック

- 記録済みの支出・収入は、誤った金額・方法・カテゴリーを後から訂正できなければならない。
- 保存時の残高だけでなく、訂正時にも差分を正しく残高へ反映する。
- 保存・送信後は、次の入力に備えて入力欄を空の状態へ戻す。
- 毎日の習慣はスマホで日本語が不自然に折り返されないことを最優先し、二列の等幅グリッドを基本とする。
- 買い物リストは、完了後にだけ左スワイプで「削除」を現す。未完了時に削除を露出させない。

## 合格条件

1. 今日の記録行を選ぶと編集画面を開ける。
2. 変更の保存後、表示内容と残高がともに更新される。
3. 画面幅 390px 前後で習慣ラベルが単語途中で分断されない。
4. 完了済み買い物を左スワイプすると削除ボタンが出て、削除後にリストから消える。
5. 公開前の確認は、必ず実表示のブラウザ画面で行う。
### 実表示確認結果（2026-08-12）

- [x] ローカル実表示で「今日を整える」から独立した「仕事の記録」を開き、作業開始・終了、休憩、プロジェクト、日報テンプレートを確認した。
- [x] 390px幅の実表示で、入力欄・チェック欄・見出し・保存導線が重ならず、作業画面がゴシックで表示された。
- [x] 休憩分が作業時間を超える保存を実表示で拒否し、記録を変更しないことを確認した。
- [x] 体調分析の実表示で「作業時間」「休憩時間」を独立指標として表示し、未記録は「未記録」と表示した。
- [x] 実表示の分析グラフで、作業・休憩の表示切替を可視操作し、現在の画面位置を保持した。
- [x] 保存操作による利用者データの追加は行わず、実データを変更しない状態で確認した。
## 2026-08-12｜時間割レーンの色トークンと共通表示の区画保証

### 利用者フィードバック

- 時間割の仕事レーンは、アイコンと「仕事」の文字を仕事色（紫）で統一する。
- 生活レーンは、アイコンと「生活」の文字を生活色（緑）で統一する。
- 「共通」表示でも、仕事の予定は仕事レーン、生活の予定は生活レーンからはみ出さない。

### 原因

レーン見出しはアイコンだけに色を設定し、文字は共通の中間色を継承していた。また、共通表示の予定カードは仕事・生活を分けずに全体の重なり列を計算していたため、カードの左位置と幅がレーン境界を越え得た。画面幅が狭いほど、長い予定名の折返しとあわせてはみ出しが再発しやすい構造だった。

### 判断・再発防止

1. 仕事色 `--flow-work-color` と生活色 `--flow-life-color` を共有トークンとして扱い、各レーンのアイコンとラベルの両方へ適用する。ページ固有の色指定は追加しない。
2. 共通表示では、予定を仕事・生活・共通のレーン単位に分けてから各レーン内の重なり列を計算する。仕事は左半分、生活は右半分、共通予定だけは全幅とし、共通の全体計算で各レーンを上書きしない。
3. カードの幅・位置は時間割シェルを基準にし、`box-sizing` と最大幅で枠内に収める。長い予定名はカード内で折り返す。
4. 修正後はCodexのローカル実表示をPC幅と390px幅で開き、色の算出値、代表カードの左右境界、横スクロール・コンソールエラーがないことを確認する。

### 実表示の合格条件

- [x] 仕事レーンのアイコンと文字が同じ紫、生活レーンのアイコンと文字が同じ緑で表示される。Codex実表示の算出色は仕事 `rgb(121,106,168)`、生活 `rgb(79,152,111)`。
- [x] 共通表示で「プラグイン開発」「ワンモーション」を含む仕事カードが仕事レーン内に収まり、生活カードは生活レーン内に収まる。
- [x] PC幅・390px幅の双方で時間割シェルから横にはみ出さず、予定タップ・編集導線を壊さない。
- [x] Codexの実表示で確認し、`ui-v2.js --check`、`git diff --check`、既存の検査一式を通過した。
## 2026-08-12｜体調記録の日付遡及・保存済み変更表示・時間割色役割

### 利用者フィードバックと対象

- 歩数・睡眠が23:59に同期されても、翌日以降に対象日を開いて保存・変更できるようにする。
- 保存済みの項目は「〇〇を保存」ではなく「〇〇を変更」と赤文字で示し、既存入力の上書きであることを伝える。
- 生活の選択ボタンと時間割の見出しロゴは生活色の緑、共通の選択ボタンと時間割ロゴは共通色のオレンジにする。時間割下部の仕事（紫）・生活（緑）のレーン色は変えない。

### 原因

体調保存のキーと画面表示日が `ymd(now())` に固定されており、同期後に過去日・翌日を選ぶ入口がなかった。そのため、同期データが存在しても当日中に保存できなければ、利用者は対象日の記録を完成できなかった。また、保存済みかどうかを項目別の操作ラベルへ反映していなかった。時間割は選択状態と見出しアイコンの色が共通の紫へ寄っており、生活・共通の役割が色で伝わりにくかった。

### 判断・再発防止

1. 体調画面に記録日セレクターを置き、表示・入力・保存のすべてで選択日を同じキーとして使う。日付変更時は未保存の下書きを混ぜず、選択した日の保存済みデータを読み直す。
2. からだ・こころ・睡眠・歩数を項目単位で保存済み判定する。未保存は「保存」、保存済みは「変更」とし、変更側だけ注意を示すソフトな赤（赤文字・薄赤背景・赤系枠）を使う。削除・危険操作の強い赤とは役割を分ける。
3. 時間割の表示モードに色トークンを割り当てる。仕事は紫、生活は緑、共通はオレンジとし、選択ボタンと時間割見出しロゴへ同じトークンを適用する。下部レーンの仕事・生活色は独立して維持する。
4. 色は装飾ではなく「現在の表示役割」「保存済みデータの変更注意」を伝える状態表現とする。主操作は日付・対象項目・保存状態が一目で分かる順に並べ、赤を常時の主操作色にはしない。

### 管理しやすさに関する設計助言

- 体調記録は「日付 → 項目 → 状態」の順で確認できると、同期の遅れや後日入力に強い。日付を画面の一番上に固定し、項目ごとに保存状態を見せる構成が管理しやすい。
- 「保存」と「変更」を同じ位置の同じ部品で切り替えると、画面を探し直さずに済む。一方で変更を赤にするのは注意喚起に限定し、入力値そのものや通常の生活色まで赤くしない方が、意味の衝突が少ない。
- 時間割は、上部の選択モード色と見出しロゴを一致させ、下部レーンの分類色を変えないことで、「今どの視点で見ているか」と「予定が何に属するか」を分離できる。

### 実表示の合格条件

- [x] Codexの実表示で記録日を `2026-08-13` へ変更し、保存対象日の切替を確認した。後日保存と「変更」切替は実データを変更しない自動検査でも確認した。
- [x] 同じ項目を再表示すると「〇〇を変更」が赤系で表示され、未保存の歩数は「歩数を保存」のままになることを実表示で確認した。
- [x] 生活表示の選択ボタン・時間割見出しロゴが緑、共通表示の選択ボタン・時間割見出しロゴがオレンジ、仕事・生活レーン色が維持されることを実測した。
- [x] 390px幅で日付変更後の値・保存状態・色が崩れず、コンソールエラーなし。既存検査と後日保存の自動検査も通過した。
## 2026-08-12｜既存の仕事予定を仕事一覧へ移行する導線

### 利用者の指摘

仕事一覧には「次にやる」「今すぐやる」「いつかやる・たぶんやる」「待ち」の枠があるが、既に設定されている「プラグイン開発」などが表示されない。既存の仕事を同じ管理単位へ移す導線が必要。

### 原因

仕事一覧は `S.workItems` だけを読み、既存の `S.areas`（プロジェクト候補）や、`S.plan`／自動生成時間割の自由テキスト予定を仕事項目として解釈していなかった。新しい「仕事内容を追加する」から登録した項目だけが `workItemId` を持つため、既存予定と仕事一覧が分断されていた。

### 判断と再発防止

- 仕事一覧に既存仕事の登録入口を置き、候補ごとに優先度と状態を選んでから登録する。ページ表示だけでデータを書き換えない。
- 登録は追加・紐付けのみとし、既存の `S.areas`、自由入力予定、日報本文、時間を削除・上書きしない。時間割の既存予定には `workItemId` と `projectId` だけを付与する。
- 自動時間割は、同じプロジェクト・名前の仕事項目が存在すれば `workItemId` を解決して表示し、仕事一覧・時間割・日報の同一性をIDで保つ。
- 同じ名前の候補を重複表示せず、登録済みの項目は候補から外す。登録後は優先度グループと時間割の仕事カードへ即時反映する。
- 読み取り専用端末では登録操作を実行できない。登録操作後も現在ページの文脈を失わない。

### 合格条件

- [ ] 仕事一覧に既存の「プラグイン開発」などが候補として実表示される。
- [ ] 候補を選び、優先度「次にやる」・状態「未着手」などを指定して登録できる。
- [ ] 登録後、仕事一覧の指定グループ、時間割の仕事カード、仕事の記録の仕事内容選択へ同じ項目が反映される。
- [ ] 元の予定・日報・プロジェクト名が消えず、同じ候補を再登録して重複しない。
- [ ] 390px幅・PC幅で枠内に収まり、登録前後にコンソールエラーがない。
- [x] 390pxのCodex実表示で既存の仕事候補4件と登録ボタンを確認し、横はみ出しがないことを確認した。
- [x] 自動検査で候補を登録し、仕事一覧への反映、候補の重複除外、既存の時間割・日報連携を確認した。
## 2026-08-12｜公開前にローカル実表示を確認する運用

利用者の希望として、公開・プッシュ前に必ずCodexのローカル実表示ページを開き、PC幅とスマホ幅で操作確認を行う。コード文字列や公開後の表示だけで合格にしない。

### 標準順序

1. HANDOFF・Git状態・版・設計正本を確認する。
2. 判断ログと操作設計票へ原因・変更対象・合格条件を記録する。
3. ローカル実表示で実装前の状態を確認する。
4. 共通トークン・構造・データ連動を実装する。
5. ローカル実表示でPC幅・390px幅・実操作・画面位置・コンソールを確認する。
6. 合格後にだけコミット、プッシュ、公開確認を行う。

この順序により、ローカルでは見えない公開キャッシュや実機差は公開後の追加確認として扱い、公開前の基本導線・レイアウト・操作不具合を先に止める。

## 2026-08-12｜プロジェクトと仕事内容の責務分離・既存選択導線

### 利用者の修正依頼

仕事一覧の管理を丈夫にするため、プロジェクトと仕事内容を混同せず、時間割から既存の仕事を選ぶ操作と新しい仕事内容を追加する操作を分ける。

### 原因

現在の仕事カタログは `S.workProjects` と `S.workItems` を持つが、既存予定の移行候補にプロジェクト名を仕事内容として扱う経路が残っている。また時間割の仕事追加フォームは新規入力しかなく、同じ仕事を再入力して重複する余地がある。

### 判断と再発防止

1. プロジェクトはまとまり、仕事内容はその中の実行単位として扱う。仕事一覧と日報の「仕事内容」にはプロジェクト行をそのままタスクとして追加しない。
2. 仕事の時間割の追加フォームは「登録済みの仕事を選ぶ」と「新しい仕事内容を追加する」を別の操作として表示する。既存選択では `workItemId` を参照し、新規追加だけが新しい `workItem` を作る。
3. 既存データは削除・自動上書きしない。既存のプロジェクト名と自由入力予定は候補として読み取り、利用者が明示的に登録したものだけを仕事カタログへ紐付ける。
4. 予定表示、仕事一覧、仕事の記録は表示名でなく `workItemId` を正本にする。プロジェクト選択を変えたときは仕事内容の候補を再絞り込み、選択中の仕事が別プロジェクトへ誤って残らないようにする。
5. 登録・選択後は共通レンダーの位置保持契約を使う。危険操作の削除はこの導線へ追加せず、保存と分離する。

### 実装前のローカル確認

- [x] `0.31.11` のローカル実表示で、仕事の時間割の仕事追加が新規入力中心で、既存仕事の選択入口がないことを確認した。
- [x] ホームから仕事の時間割・仕事の一覧・仕事の記録へ遷移できることを確認した。

### 合格条件

- [ ] 仕事の時間割で「登録済みの仕事を選ぶ」と「新しい仕事内容を追加する」が視覚・文言とも別入口になる。
- [ ] 登録済みの仕事を選んで予定を追加すると、既存 `workItemId` のまま時間割・仕事一覧・日報の仕事内容選択へ反映される。
- [ ] 新しい仕事内容を追加した場合だけ `workItem` が1件増え、同名の既存仕事を重複作成しない。
- [ ] プロジェクト名だけが仕事内容として新規作成されず、既存プロジェクト・予定・日報本文は失われない。
- [ ] 保存・選択後の画面位置を保持し、390px幅とPC幅で選択欄・追加欄・時間割カードが枠内に収まる。
- [ ] `tools/check.ps1`、`node --check ui-v2.js`、コンソールエラーなしを確認し、実表示で既存選択と新規追加を各1回検証する。

### 実装後の確認結果（2026-08-12）

- [x] ローカル実表示で、仕事追加の種類が「登録済みの仕事を選ぶ」「新しい仕事内容を追加する」に分かれ、選択時は新規仕事内容入力が表示されないことを確認した。
- [x] ローカル実表示で、プロジェクト参照枠と仕事内容の優先度グループが別に表示されることを確認した。
- [x] `tools/check.ps1` で新規仕事内容の作成後、登録済み仕事を選んだ予定が同じ `workItemId` を再利用することを確認した。
- [x] `node --check ui-v2.js`、`git diff --check`、版整合、コンソールエラーなしを確認した。
- [ ] 公開後のSafari実機確認は、公開工程で追加確認する。

## 2026-08-12｜優先度順と時間割カード表示の共通化

### 利用者の修正依頼

仕事一覧の優先度を「今すぐやる」「次にやる」「いつかやる・たぶんやる」「待ち」の順にする。「今すぐやる」は最重要として赤にする。時間割の仕事カードは仕事内容と時間だけを表示し、優先度に応じた共通色で仕事一覧と連動させる。

### 原因

優先度の定義配列が `next` → `now` の順で、仕事一覧の表示順が利用者の行動順と逆だった。また時間割はプロジェクト・優先度・状態をカード内の補足文字へ一括表示していたため、時間と仕事内容をすばやく読む目的に対して情報が過多になり、カード色もプロジェクト色を使って仕事一覧の優先度と連動していなかった。

### 判断と再発防止

1. 優先度の正本配列を `now`（今すぐやる）→ `next`（次にやる）→ `someday`（いつかやる・たぶんやる）→ `waiting`（待ち）へ固定する。既存IDとA〜D互換値は変更しない。
2. 優先度色は共通トークンとして `now=#c85d54`（赤）、`next=#d2a449`（黄）、`someday=#4d80ad`（青）、`waiting=#796aa8`（紫）を使い、仕事一覧の枠と時間割の仕事カードへ同じ値を渡す。状態色は別の意味なので変更しない。
3. 時間割の仕事カードは仕事内容名と開始・終了時刻だけを表示する。プロジェクト、優先度、状態は仕事一覧・編集・日報で確認し、時間割へ再掲しない。
4. 色は表示名やプロジェクト色から推測せず、`workItemId` から解決した優先度に対して共通関数で決定する。優先度変更後の再描画で時間割の色も更新する。

### 合格条件

- [x] 仕事一覧が「今すぐやる」「次にやる」「いつかやる・たぶんやる」「待ち」の順で表示される。
- [x] 「今すぐやる」の仕事一覧枠と時間割カードが同じ赤色になり、他3分類も一覧と時間割で一致する。
- [x] 時間割の仕事カードには仕事内容名と開始・終了時刻だけが表示され、プロジェクト・優先度・状態の補足文字が出ない。
- [x] 仕事一覧から優先度を変更して保存すると、該当グループと時間割カード色が同時に更新される。
- [x] 390px相当の横幅条件・PC幅で文字がカード外へ出ず、保存・選択後の位置保持契約とコンソールエラーなしを確認した。

### 実装後に発見した互換不具合

既存データを読み込んだローカル実表示で、「プラグイン開発」などが仕事一覧では「今すぐやる」なのに、時間割では優先度色へ連動しなかった。

#### 原因と再発防止

- 前回のプロジェクト／仕事内容分離で、旧形式の `workItems` に `kind` がない場合を未分類として扱い、同名の自動時間割枠とのID解決を止めていた。
- `normalize()` で既存 `workItems` の欠落 `kind` を互換的に `task` へ正規化する。自動時間割は明示的にカタログへ登録された `task` だけを `workItemId` で解決し、プロジェクト配列だけからは仕事内容を生成しない。
- これにより既存の優先度・状態・プロジェクト名・予定を変更せず、一覧と時間割が同じ仕事内容ID・優先度色を参照する。

#### 追加合格条件

- [x] 旧形式の仕事項目を読み込んでも、仕事一覧の優先度と同じ色が対応する時間割カードへ表示される。
- [x] 旧形式の仕事項目を読み込んでも、保存データの名称・優先度・状態・予定時刻が変わらない。

### 実表示確認結果（2026-08-12）

- [x] ローカル実表示 `0.31.14` で「プラグイン開発」が一覧の「今すぐやる」赤色と時間割の赤色で一致した。
- [x] 同じ実表示で時間割カードの文言が「プラグイン開発」「10:00 - 12:00」の2行だけになった。
- [x] 一覧の順序と4分類の色、既存データの互換連動、横はみ出しなしを確認した。
## 2026-08-12｜優先度色の時間割連動と新規ユーザー向け表示

### 利用者の修正依頼

時間割の仕事カードも仕事一覧と同じ重要度の色で表示する。仕事一覧の「今すぐやる」は赤いグループであることを保ちつつ、赤い縦線が連続して左側へ集中する見た目は避ける。開発中にしか分からない情報ではなく、新規ユーザーが次の行動を理解できる文言にする。

### 原因

優先度色をグループ枠と各仕事内容の左辺へ同時に付けたため、同じ色の縦線が重なり、視線の重心が左へ寄った。時間割は仕事一覧の優先度を参照していたが、画面上の色の意味を共通トークンで説明する構造と、初回利用者向けの案内文が不足していた。また、`workItemId`、同じカタログ、元データなどの実装・移行上の語をそのまま表示すると、利用者の目的（仕事を整理して次にすることを見る）より開発者の事情が前面に出る。

### 判断・変更順序

1. 優先度の共通色トークンを正本にし、時間割の仕事カードと仕事一覧のグループ見出しで同じ色を参照する。
2. グループは上辺と見出しの小さな印で優先度を示し、内側の仕事内容カードは中立の枠線と薄い背景で受ける。赤いグループの識別性は残し、赤い縦線の連続は作らない。
3. 画面文言は、内部ID・互換・カタログ・元データを説明せず、「何ができるか」「次に何をするか」を先に伝える。
4. 実装後にローカルの実表示で時間割・仕事一覧をPC幅と390px相当で確認し、色、文言、枠内への収まり、コンソールエラーを確認する。

### 再発防止

- 優先度色は画面ごとに個別指定せず、共通トークンからのみ取得する。
- 同一画面内で同じ強い色を縦方向に連続配置しない。強調は外枠の一箇所と小さな印に分散し、内容カードは読みやすさを優先する。
- 新しい画面文言は「新規ユーザーがその場で理解できるか」を確認し、実装・移行・開発者向けの用語を利用者向け説明へ持ち込まない。
- 合格確認はコード文字列だけで終えず、実ブラウザの表示と操作で行う。

### 実表示での合格条件

- 時間割の仕事カードは、仕事一覧の優先度と同じ色で表示され、カード内は仕事内容と時間だけである。
- 「今すぐやる」は赤い上辺と見出しの印でグループとして認識でき、赤い左辺が仕事内容の件数分連続しない。
- 仕事一覧・仕事内容追加の案内は、新規ユーザーが行動を理解でき、`ID`、`カタログ`、`元の予定`など開発者向けの語を表示しない。
- 390px相当およびPC幅で文字・カードが枠外へはみ出さず、保存・選択後も表示位置を失わない。
- 390px相当およびPC幅で文字・カードが枠外へはみ出さず、保存・選択後も表示位置を失わない。

## 2026-08-12｜意味色を面・枠・印で共通表示

### 利用者の修正依頼

時間割で行った「優先度色を左辺だけでなく薄い色面と枠でも伝える」改善を、意味色を持つ画面部品全体へ適用する。色が設定されているのにカード面がほぼ灰色のままになる状態をなくす。

### 原因

色を示す場所が左辺や小さな文字に限られ、カード面は共通の灰色・中立色で固定されていた。そのため、同じ色の意味が画面ごとに弱くなり、色分けが装飾に見えていた。さらに、画面ごとに個別の背景色を足すと、色の濃さやコントラストがばらつき、スマホ幅での可読性を損なう。

### 判断・変更順序

1. 意味色の正本を共通トークンに置き、色相は変えずに面色・枠色・アクセントへ展開する。
2. 優先度・カテゴリ・状態を持つカード、選択肢、時間割項目へ同じ展開規則を適用する。意味を持たない説明パネルや入力欄は中立面のままにする。
3. 面色は薄く、文字色はゴシックの基本色を維持し、枠または小さな印で境界を補強する。強調色を全面ベタ塗りにしない。
4. PC幅と390px相当の実表示で、色の意味、文字の読みやすさ、枠内への収まり、操作後の位置保持を確認する。

### 再発防止

- 意味色を使う新しい部品は、共通トークンから面色・枠色・アクセントを組み立てる。
- 灰色の固定背景を意味色付き部品へ追加しない。中立面を使う場合は、意味色の枠または印を必ず併用する。
- 色の変更は画面単体で判断せず、同じ意味を持つ一覧・時間割・分析の実表示で比較する。
- 合格判定はCSS値の確認だけで終えず、実ブラウザのPC幅・スマホ幅相当で目視と機械検査を行う。

### 実表示での合格条件

- 意味色を持つカード・選択肢・時間割項目は、共通トークンと一致する薄い面色、枠、アクセントの組み合わせで表示される。
- 文字と数字のコントラスト、1:1の基本比率、ゴシック表示を保ち、色を付けても内容の読みやすさを失わない。
- 390px相当およびPC幅で、面色・枠・印が重なって濁らず、カードが枠外へはみ出さない。

## 2026-08-12｜仕事グループの視認性再調整

### 利用者の修正依頼

仕事グループの面色が濃く、仕事の時間割・一覧・記録が同じ青面で連続して視認性が落ちた。仕事グループは今より薄い青で示し、内側の3つの入口は左帯だけで仕事色を示す構成へ戻す。

### 原因

意味色を面色へ一律に展開した結果、同じ仕事色のカードが隣接し、グループのまとまりと個別入口の境界が弱くなった。色の意味を増やした一方で、情報の階層（仕事というまとまり／時間割などの入口）を分けていなかった。

### 判断・再発防止

1. グループは薄い青面と見出しで仕事領域を示す。
2. グループ内の仕事入口は中立の面に戻し、左帯だけを青にして、入口同士の文字と枠のコントラストを確保する。
3. 意味色を追加するときは、面色を足す前に「グループの色か、個別項目の色か」を区別し、同じ色面を連続させない。
4. 実表示では視認性（文字・枠・境界）を、色の一致やはみ出しとは別の合格項目として確認する。

### 実表示での合格条件

- 仕事グループは薄い青で認識でき、仕事の3入口は中立面＋青い左帯で読める。
- 仕事・生活・見える化のグループ境界と、各入口の境界が一目で分かる。
- 390px相当／PC幅で文字・数字・枠のコントラストを保ち、横はみ出しがない。

## 2026-08-12｜全トップグループの視認性調整

### 利用者の修正依頼

仕事グループの青をさらに薄くし、同じ階層ルールを生活・見える化にも適用する。グループは薄い色でまとまりを示し、個別入口は中立面と左帯で読みやすくする。

### 原因

仕事グループを薄くした後も、生活・見える化はグループと個別入口の色の役割が揃っていなかった。画面ごとに色面の濃さが異なると、領域のまとまりと操作対象の境界を同じ基準で読み取れない。

### 判断・再発防止

1. 仕事・生活・見える化のグループ背景は、それぞれの意味色を極薄くした共通グループ面で統一する。
2. 各グループ内の入口は中立面に戻し、個別の意味色は左帯へ集約する。生活内の習慣など、別の意味を持つ項目の帯色は保持する。
3. 色の濃さはグループ面より文字・枠・左帯を優先し、同じ面色が連続して視認性を下げないようにする。
4. 3グループを同じ実表示で比較し、見出し・個別入口・文字のコントラストを確認する。

### 実表示での合格条件

- 仕事はさらに薄い青、生活は薄い緑、見える化は薄い紫のグループ面で表示される。
- 各グループの個別入口は中立面＋意味色の左帯で、文字と枠が読みやすい。
- 生活内の習慣、見える化内の分析など、個別の意味色が失われない。
- 390px相当／PC幅で視認性、階層、枠内収まりを確認する。
- 390px相当／PC幅で視認性、階層、枠内収まりを確認する。

## 2026-08-12｜全トップグループの視認性調整の撤回

### 利用者フィードバック

仕事の色をさらに薄くし、生活・見える化へ同じ階層ルールを広げた表示は、実表示で全体のまとまりが微妙になったため、変更前へ戻す。

### 判断

0.31.18の生活・見える化への適用と、仕事グループの追加希釈を撤回する。0.31.17の表示（仕事グループのみ薄い青、仕事の入口は中立面＋青い左帯、生活・見える化は変更前の表示）を正本へ戻す。記録・データ構造・操作導線は変更しない。

### 再発防止

- 3グループへ同時に色構造を広げる前に、グループごとの実表示と全体の重心を比較する。
- 色の濃さだけでなく、まとまり・階層・既存画面との調和を合格条件に含める。
- 利用者が「微妙」と判断した表示は、別案を積み重ねず、直前の確認済み版へ戻してから再設計する。

### 実表示での合格条件

- 0.31.17相当の表示へ戻り、生活・見える化の既存表示が変わらない。
- 仕事グループの薄い青と左帯の視認性が維持される。
- 390px相当／PC幅で、戻した後の境界・文字・枠・画面位置に崩れがない。
- 390px相当／PC幅で、戻した後の境界・文字・枠・画面位置に崩れがない。

## 2026-08-12｜ホーム画面だけを変更前へ戻す

### 利用者フィードバック

前回の撤回では戻す範囲が広く、ホーム画面以外の確認済み変更まで戻ったように見えた。ホーム画面の色面・グループ階層だけを変更前へ戻し、時間割・仕事一覧・共通色トークンの改善は維持する。

### 判断・再発防止

1. 差分を「ホーム画面の入口表示」と「ホーム画面以外の共通部品・時間割・仕事一覧」に分けて扱う。
2. ホーム画面は変更前の中立面＋左帯へ戻す。時間割カードの薄い色面、仕事一覧の優先度構造、共通トークンは変更しない。
3. 戻し作業後は、ホームだけでなく時間割と仕事一覧を開き、意図しない巻き戻りがないことを実表示で比較する。

### 実表示での合格条件

- ホームの仕事・生活・見える化の入口が変更前の中立面＋各色の左帯になる。
- 時間割の優先度色面・枠、仕事一覧の優先度グループ、共通トークンは現行の改善状態を維持する。
- 390px相当／PC幅で、ホーム・時間割・仕事一覧に枠外、文字欠落、位置ずれがない。

## 2026-08-12｜仕事ログの複数プロジェクト記録と共通時刻軸

### 利用者フィードバック

一日に複数の仕事を同時並行で進めるため、「仕事を選ぶ」は複数プロジェクトを選択でき、各プロジェクトの仕事内容を記録できるようにする。日報テンプレートは利用者向けの名称「仕事の内容」に変更し、やったこと・成果・今の状況などは定型選択と自由入力を併用する。PCでは正しく見えてもスマホで日付・時間割・予定編集の時刻がずれるため、表示計算とネイティブ日付・時刻入力の見せ方を共通化する。

### 原因

- 仕事ログが単一の `projectId`／`workItemId` だけを保存する構造で、同時並行の仕事を一つの記録に保持できない。
- 日報本文が自由記述だけで、状態や成果の分類が一覧・分析へつながりにくい。
- 時間割のJavaScript計算が1時間28px、CSSの目盛りが1時間49pxで別々に定義され、ブラウザ幅や実表示でラベル・カード・罫線がずれる余地がある。
- `input[type=date/time]` のネイティブ内部描画をそのまま表示しており、Safariの内部余白・アイコン・文字基線差を吸収できていない。

### 判断・再発防止

1. 複数プロジェクトは配列で正本化し、旧単一IDも互換用に残す。各プロジェクトに仕事内容のドロップダウンを持たせ、既存保存データと未選択状態を壊さない。
2. 仕事の内容は定型選択を基本にし、選択肢にない内容だけ自由入力へ切り替える。保存時は既存の `done` などの項目名を維持して、履歴・分析との整合性を保つ。
3. 時間割の1時間高さを共通トークン `FLOW_HOUR_PX` とCSS変数で一元化し、ラベル・予定カード・現在線・背景罫線が同じ軸を使う。カードは表示領域内で幅をクランプする。
4. 日付・時刻は実入力を透明な操作層、表示文字を共通の中央表示層に分ける。Safari固有の内部描画を見せず、フォーカス時も入力枠と操作性を維持する。
5. 保存・日付変更・選択後は同じページとスクロール位置を保持し、危険操作（削除）は保存から分離する。

### 実表示での合格条件

- 390px相当とPC幅で、複数プロジェクトを選択し、それぞれの仕事内容を選んで保存・再表示できる。既存の単一プロジェクト記録も表示される。
- 「仕事の内容」が画面上の利用者向け見出しになり、成果・状況・次の作業などを選択式で入力でき、自由入力へ切り替えても既存値が失われない。
- 390px相当とPC幅で、時間割の時刻ラベル・背景罫線・予定カード・現在線が同じ1時間軸に揃い、予定カードが左右・上下の枠外へ出ない。
- 390px相当とPC幅で、仕事ログ・体調記録・予定編集の日付／開始／終了が中央表示され、Safariの表示差で文字やアイコンがずれない。
- 画面操作後も選択した日付・スクロール位置を失わず、コンソールエラーがない。

### 0.31.21 実装後確認

- [x] ローカル390px相当で、4プロジェクトのチェック選択と、選択した各仕事内容ドロップダウンの有効化を実操作で確認した。
- [x] ローカル390px相当で、「仕事の内容」の定型選択と「自由に入力する」の表示切り替えを確認した。
- [x] ローカル390px相当で、時間割の時刻ラベル間隔が49px、10:00カードが4:00から294px位置、13:30カードが465.5px位置となり、計算軸が一致していることを照合した。
- [x] ローカル390px相当で、予定編集ダイアログを中央表示し、開始10:00・終了12:00が左右同じ構造で表示されることを確認した。
- [x] PC幅で時間割の1時間49px、タイムライン幅263px、横はみ出しなしを照合した。コンソールの警告・エラーは0件だった。
## 2026-08-12｜仕事の選択UIと時間割・入力枠の共通表示修正

- 依頼・判断: 「仕事を選ぶ」はチェックボックス一覧をやめ、追加用ドロップダウンから複数の仕事を登録し、選択済みの仕事を一覧で確認できる構造にする。日付・開始・終了の入力枠、および時間割の外枠・時刻列がPCとスマホで同じように見えることを必須とする。
- 原因: 選択状態が各プロジェクト行のチェックボックスに埋もれており、複数選択後の確認面がない。ネイティブ入力を透明化した一方で枠・背景をラッパーへ移していなかったため、枠が消えた。時間割は時刻列をタイムラインの外側へ出したまま、親のクリップと余白・境界を別々のCSS層で上書きしていたため、PC幅でも時刻列と外枠が消え得る。
- 再発防止: 選択UIは「追加・選択済み一覧・外す」を共通部品として扱い、保存時は配列を正本にして既存データを復元する。ネイティブ入力は操作層に限定し、見た目の枠・背景・高さは共通フィールドシェルが所有する。時間割は外枠が時刻列の領域とイベント領域を内包し、時刻・カード・1時間の高さを同じトークンで計算する。390px幅とPC幅の実表示、2件以上の追加、削除・再表示、コンソールエラー0を毎回確認する。
- 実表示の合格条件: 追加用ドロップダウンから2件以上を追加でき、選択済みの仕事名と仕事内容が表示され、外しても他の選択を失わない。保存済みの複数選択が再表示される。日付・開始・終了の枠がPC/390pxで表示され、値が中央に揃う。時間割の外枠・時刻列・カードが表示され、時刻とカードの位置が49px/時で一致し、カードが枠外へはみ出さない。
## 2026-08-12｜アプリ資産のワークスペース分離

- 依頼・判断: このアプリのソース、設計資料、検証資料を `Skeleton` から分離し、`D:\仕事用\5_その他\mainichi` を唯一の作業正本とする。
- 対象: 検証画像3点は `private\qa`、UI仕様書と生成スクリプトは `private\life-note-spec` へ移動した。生成スクリプトの出力先も移動後の仕様書へ更新した。
- 除外: `couple-diary`、`pairly-v2`、PDF作業一式、`Skeleton` の `.git` は別アプリ・別作業・別Git境界のため移動しない。
- 再発防止: 以後の開始確認では `mainichi` のGit状態・`private/HANDOFF.md`・`design/` を正本として確認する。移動時は対象ファイルを個別指定し、移動先が `mainichi` 配下であること、既存ファイルがないことを検査してから実行する。`private/` の資料は公開物へ含めない。
- 合格条件: 対象5点が `mainichi` 配下に存在し、元の `Skeleton` 側に残っていない。仕様書生成スクリプトの出力先が新しい場所を指す。`mainichi` のGit境界を壊さず、別アプリの資産を混入させない。
## 2026-08-12｜時間割カード内の時刻がずれて見える再発原因

- 依頼・診断: 時間割の「プラグイン開発」などで、開始時刻とカード内の文字が一致せず、全画面に共通する可能性を調査した。
- 実測: 390px幅の実表示で、10:00カードの上端は時間レールの10:00位置と一致し、カードの `top` は294px、1時間の軸は49pxだった。データ変換や分計算のずれではない。
- 原因: イベントカードを `button` で描画し、長いカードの内部内容をブラウザのボタン内部配置に任せていたため、タイトルと時間がカードの高さ方向の中央へ寄った。2時間カードでは内容が約29px下がり、10:00のカードなのに11:00付近から始まったように見える。短時間カードより長時間カードで目立つ。
- 再発防止: 時間割カードは「位置」と「内容配置」を別契約にする。位置は `FLOW_HOUR_PX` で決め、内容は共通カード構造の上端固定（タイトル→時間）を明示する。`button` のブラウザ固有内部配置に依存せず、PC/390pxで1時間・2時間・10分のカードを実測する。時間入力や編集ダイアログも同じく、値の中心と表示位置を別々に確認する。
- 実表示の合格条件: 10:00–12:00カードの上端・タイトル・時間表示が10:00位置から始まり、内容がカード中央へ移動しない。13:30–16:00、14:10–14:20、19:30–21:00でも同じ規則が成立し、PC/390pxでカード上端とレールの差が0px、横はみ出しとコンソールエラーがない。

### 実装判断（0.31.22修正）

- 実装方針: 時間割イベントの `button` を共通の縦積みレイアウトにし、タイトルと時刻を上端から同じ余白で配置する。リサイズ用の上下ハンドルは絶対配置の操作層として内容層から分離する。
- 変更範囲: 仕事・生活・共通のすべての時間割カードに適用する。個別カードの `top`、高さ、色、レーン位置の応急処置は行わない。
- 検証: 修正後にPC幅と390px幅で、長時間・中時間・短時間のカードを実表示し、カード上端／タイトル／時刻の相対位置、操作ハンドル、横はみ出し、コンソールエラーを確認する。

### 実装後確認（0.31.22）

- [x] PC幅と390px幅で、イベントカードの表示を共通 `flex` 構造に変更し、タイトルのカード上端からの距離が8pxで揃った。
- [x] 10:00–12:00、13:30–16:00、19:30–21:00で、カードの高さに関係なくタイトルが中央へ移動しないことを確認した。時刻表示はタイトル直下に固定された。
- [x] 390px幅で、10:00カード上端と10:00ラベルが同じ位置（差0px）になった。
- [x] PC幅・390px幅とも横スクロールなし。リサイズ用ハンドルは内容層と分離されたまま表示される。

### DS-FLOW-006 追補｜短時間カードの時刻が下端で隠れる診断（実装前）

- 依頼・診断: 生活予定「傷病手当の日付訂正依頼」は時刻データを持っているのに、スマホ表示で「10:00 - 10:30」が見えない／下端で切れて見える。
- 実測: DOMの文字列は「傷病手当の日付訂正依頼」「10:00 - 10:30」とも存在する。カード高さは42px、上下paddingは各8px、タイトルは約16.8px、時刻は約13.2px、間隔は3pxで、上下paddingを含む本文の必要高さが最小カード高を上回っている。カードのoverflowはhidden。
- 原因: データ欠落や時間計算ではなく、30分予定の最小カード高さが、タイトル・時刻・間隔・上下paddingを同時に収める高さになっていないため、時刻がカード下端に接し、スクリーンショットの切り取りやoverflowで欠落して見える。
- 再発防止: 短時間カードの最小高さを固定値だけで決めず、共通カード構造の本文高さ（タイトル＋時刻＋間隔）と上下paddingから算出する。10分・30分・1時間以上をPC／390pxの実表示で確認し、DOM文字列の存在とスクリーンショット上の全文表示を別々に合格判定する。
- 実装前の合格条件: 30分予定のタイトルと開始・終了時刻がカード内に全文表示され、下端で切れない。時間軸、カード上端、位置保持、横収まり、コンソールエラーに回帰がない。

### 実装後確認（0.31.22）

- [x] 仕事・生活・共通の全時間割カードへ共通最小高50pxを適用した。
- [x] 390px実表示で「傷病手当の日付訂正依頼」の「10:00 - 10:30」がカード内に全文表示された。
- [x] スクロール後の実表示で、仕事・生活カードのタイトルと時刻が下端で切れず、横収まり・コンソールエラーに回帰がない。

## 2026-08-12｜時間レールの重なりと体調入力枠の数値表示再発原因

- 依頼・診断: 時間割の時刻数字がカードに入り込み、体調記録の歩数・睡眠時間・日付枠も表示が不自然に見える。仕事の記録の作業時間表示は正常なため、共通入力全体を壊さず画面ごとの構造差を確認する。
- 時間割の原因: 共通表示のカード位置を `--event-left`／`--event-width` の `calc()` で持ち、CSS側で時間レール幅をさらに `calc()` に加算していた。変数内に `calc()` を含む入れ子式が無効になり、390px表示では実際のカードが時間レール内（左96px、レール終端131px）から始まっていた。
- 体調記録の原因: 全画面向けの透明ネイティブ入力規則（高さ52px・高さ100%）が、体調記録の小型日付／睡眠入力にも適用され、外側の枠が70pxに膨らんでいた。作業記録は専用の52pxコントロールを持つため同じ不具合が出ていない。歩数・睡眠の保存値にも専用の数値書式指定が不足していた。
- 再発防止: 時間割は「時刻レール」と「カード領域」をDOM上で分離し、カード領域を基準にレーン位置を計算する。CSSで座標式を再合成しない。入力は用途ごとにサイズトークンを分け、体調のコンパクト入力は明示的な高さ・最小高さを持たせ、保存値は作業記録と同じ数字トークン（ゴシック、tabular/lining numbers）を使う。
- 実表示の合格条件: 390px／PC幅の共通・仕事・生活表示で時刻ラベルとカードが重ならず、仕事カードの左端が時間レール終端より右にある。体調記録の日付枠・睡眠時刻枠・歩数／睡眠値が用途別の枠内に収まり、作業記録の開始／終了表示を変えない。横はみ出し・コンソールエラーがない。

### 実装後確認（0.31.22）

- [x] 390pxの共通表示で、時間レール終端131pxに対し仕事カード左端141px、生活カード左端237.5pxとなり、時刻数字との重なりがなくなった。
- [x] 390px／PC幅の仕事・生活表示でもカード左端がレール終端より右に出ることを確認した。
- [x] 体調記録の日付枠を52px、睡眠時刻枠を38pxに統一し、歩数・睡眠時間へ作業記録と同じ数値書式を適用した。
- [x] 仕事の記録の作業開始・終了枠は54pxのまま回帰なし。各表示で横はみ出しなし。
- [x] `tools/check.ps1` が版一致、レンダー入口、コンソールエラーなし、画面遷移を含めて通過した。

## 2026-08-13｜時間割の中央線と編集時刻の座標系再発原因

- 依頼・診断: 「仕事／生活」の見出し中央線と、時間割内の中央線が同じX座標になっていない。予定編集の開始・終了時刻も、同じ中央揃え原則を明示して再確認する。
- 原因: レーン見出しは時間割全体（時刻レールを含む、390px幅でX=86〜334、中央X=210）を基準にしていた。一方、中央線を `an-flow-event-layer:after` に置いたため、時刻レールを除いたカード領域（X=131〜334、中央X=232.5）を基準に描画され、22.5px右へずれた。
- 編集時刻: 開始・終了の入力枠と表示値は各枠の中央に配置されているが、画面幅やブラウザのネイティブ時刻入力に依存しないよう、2列グリッド・同一幅・同一中央表示層を共通契約として固定する。
- 再発防止: 中央線・レーン見出し・時間ラベルは、時刻レールを含む同じ時間割フレームの座標系だけを使う。カードの内容レイヤーはカード配置専用とし、フレーム全体の装飾線を置かない。編集時刻は開始／終了を同じグリッド列・同じフィールドシェル・同じ数値トークンで表示する。
- 実表示の合格条件: 390px／PC幅で「仕事／生活」見出しの境界線と時間割中央線のX差が0px。時刻ラベルは時刻レール内、カードはレール外に収まる。編集画面の開始／終了枠は等幅で、値の中心が各枠の中心と一致し、画面位置・横幅・コンソールエラーに回帰がない。

### 実装後確認（0.31.22）

- [x] 390px幅で、時間割全体の中央Xとレーン見出しの境界Xがともに約211px、差0pxになった。中央線は時間割フレームの `50%` に移動した。
- [x] 仕事・生活・共通の各表示で同じ中央線契約を確認し、カードは時刻レール外、横はみ出しなしだった。PC幅でも中央X差0pxだった。
- [x] 予定編集の開始／終了は同一2列グリッドで、各表示値の中心と各入力枠の中心の差0px。表示値は枠の上下中央にも揃った。
- [x] `tools/check.ps1` 通過、コンソールエラーなし。

## 2026-08-13｜時間レールが左端からずれた再発原因

- 依頼・診断: 中央線は揃ったが、時間割の時間表示が以前の左端位置から右へずれている。元の配置原則「時間は時間割フレームの左端」を回復する。
- 原因: 時刻レールを `v2-timeline` 内の専用レイヤーへ移した後も、旧CSSの `.an-flow-timeline-shell{padding-left:45px}` が残っていた。さらにタイムライン内にもレール幅45pxを持たせたため、親の余白と内部レールを二重計上し、時間表示全体が左端から45pxずれた。
- 再発防止: 座標の所有者を一つにする。時間割シェルは左右余白を持たず、時間割フレームが左端から時刻レール45pxとカード領域を内包する。旧レイアウトの余白を残さない。実測表では「外枠左端・時間レール左端・カード領域左端・中央線」を同時に確認する。
- 実表示の合格条件: PC／390pxで時刻レール左端が時間割フレーム左端と一致し、カードはレール終端より右に始まる。レーン見出し境界と中央線のX差0px。ラベル・カードの時間位置、横はみ出し、コンソールエラーに回帰がない。

### 実装後確認（0.31.22）

- [x] シェルの旧45px余白を除去し、時間レールをフレーム左端へ戻す。

### 実装後確認（0.31.22）

- [x] 390px幅で時間割フレーム左端と時間レール左端がX=41で一致した。カードはレール終端X=86より右のX=96から始まった。
- [x] 390px幅で時間割中央Xとレーン見出し境界Xがともに187.5pxで一致した。PC幅でも同じ一致を確認した。
- [x] 予定編集の開始／終了値は各入力枠の水平方向・垂直方向の中心差0pxだった。
- [x] 横はみ出しなし。既存の通し検査を再実行する。

## 2026-08-13｜仕事・生活カード領域の中央線へ基準を変更

- 依頼・判断: 時刻ラベルを含む時間割枠全体の中央に線を置くと、仕事・生活カード領域の中央と一致せず、予定の大きさ・見え方が崩れる。縦点線は仕事カード領域と生活カード領域のちょうど中央に置く。
- 実測: 390px幅で時間割全体はX=41〜334、時刻レールはX=41〜86、カード領域はX=86〜334。現在の見出し境界・中央線はX=187.5、カード領域中央はX=210だった。
- 原因: 見出しと中央線がフレーム全体を基準にし、カード配置が時刻レールを除いた領域を基準にしていた。2つの正しい座標系を混同していた。
- 設計判断: 時刻ラベルは左端の時間レールに固定する。仕事・生活の見出しと縦点線だけはカード領域を2等分する。カードの幅・左端・時間軸は変更しない。
- 再発防止: 「フレーム全体」「時刻レール」「カード領域」を別トークンとして扱い、中央線の所有者をカード領域へ固定する。実測表でカード領域の左右端・中央線・見出し境界を同時に確認する。
- 実表示の合格条件: PC／390pxで、見出し境界X＝カード領域中央X＝縦点線X。仕事・生活カードの左右幅と時間位置は変更前と同じ。時刻レールはフレーム左端、横はみ出し・コンソールエラーなし。

### 実装後確認（0.31.22）

- [x] 390pxでカード領域はX=86〜334、中央X=210。見出し境界と縦点線もX=210に一致した。
- [x] PC幅でもカード領域はX=523.5〜786.5、中央X=655。見出し境界と縦点線もX=655に一致した。
- [x] 390pxの予定カードは仕事X=96・幅112、生活X=215・幅112で、中央線の変更前後にカード幅・時間位置の差分がない。横はみ出しもない。
- [x] ローカル実表示を390px・PC幅で確認し、コンソールエラーなし。時間ラベルはフレーム左端の時間レールに残った。

## 2026-08-13｜時間割からチェックリスト導線を分離し、週／月カレンダーを実用化

- 依頼・判断: 時間割内の「チェックリスト」は生活側に既に入口があるため、時間割の主操作から外す。週／月カレンダーは日付の印だけでは実質的に予定確認ができないため、習慣を含む予定の確認・負荷調整・先の見通しに使える画面へ改める。
- 既存実装の原因: 現行カレンダーは月の日付と予定点を描画するだけで、期間移動、選択中日の予定一覧、毎日予定の意味表示がなく、週表示も予定の重なりや負荷を比較できなかった。時間割には、別導線で使えるチェックリストへのボタンが残っていた。
- 調査結果: 習慣は同じ文脈で繰り返すほど自動化が進み、個人差は大きいが継続した文脈が重要である（Lally et al., 2010）。また、いつ・どこで・何をするかを事前に具体化する実行意図は、目標達成を高める効果がメタ分析で示されている（Gollwitzer & Sheeran, 2006）。そのため、週表示は「今週の負荷・重なり・毎日の習慣」を調整する面、月表示は「支払い・通院・締切など先の予定と習慣の配置」を見通す面として役割を分ける。
- 設計判断: カレンダーは共通の期間モデルから、①前後移動・今日へ戻る、②すべて／仕事／生活／共通の絞り込み、③週表示の日別予定一覧（毎日予定は「毎日」と表示）、④月表示の日付ごとの予定数・色印・次の予定概要、⑤日付タップで同じ日付の時間割へ移動、を提供する。表示切替・期間移動・絞り込みはデータを変更せず、現在位置を保持する。チェックリストは生活側の既存入口を正本とし、時間割には重複導線を置かない。
- 再発防止: 週／月の表示データを同じ `allBlocks` と期間キーから生成し、カレンダーだけ別の予定判定を持たない。日付セルの印だけを合格にせず、期間移動・絞り込み・毎日予定・日付から時間割への遷移を実表示で検査する。主操作と補助導線を画面ごとに棚卸しし、同じ目的の入口を重複させない。
- 実表示の合格条件: PC／390pxで、時間割にチェックリストボタンがない。週／月の切替、前後移動、今日へ戻る、仕事／生活／共通の3種の絞り込みが動作し、週では日別の予定名・時刻・毎日表示、月では予定数・色印・次の予定が読める。日付タップで同じ日付の時間割が開き、横はみ出し・位置喪失・コンソールエラーがない。
- 参照: [Lally et al. (2010)](https://doi.org/10.1002/ejsp.674)、[Gollwitzer & Sheeran (2006)](https://doi.org/10.1016/S0065-2601(06)38002-1)

## 2026-08-13｜時間割カードと中央点線の左右余白を同一トークンに固定

- 依頼・診断: 週表示は採用するが、共通表示の仕事／生活カードと中央の縦点線について、カードの中心側の端から点線までの距離が左右で違って見える。見出し下の短い縦点線も同じX座標であることを維持する。
- 実測（PCローカル表示）: カード領域はX=523.5〜786.5、見出し境界とイベント層の点線はX=655.0で一致していた。一方、仕事カードは右端X=653.0（点線まで2px）、生活カードは左端X=660.0（点線まで5px）で、中心側の余白が非対称だった。
- 原因: レーン位置の計算が仕事側に+10px、生活側に+5pxを別々に持ち、幅も共通の-12pxだった。見出しと点線の基準は正しいのに、カードの中心側の端だけが別の余白規則になっていた。
- 設計判断: 中央線はカード領域の中央を所有し、仕事・生活カードの中心側余白は共通のFLOW_LANE_GAP相当の同一値で算出する。レーン見出しの境界・イベント層の点線・カード端の測定を同じ実測表で確認する。個別カードの座標補正は行わない。
- 再発防止: 左右のレーンで異なるマジックナンバーを持たせない。leftの内側余白とwidthの両端余白を同じトークンから生成し、PC／390pxで「仕事右端→点線」と「点線→生活左端」の距離を比較する。
- 合格条件: PC／390pxで見出し下の境界、中央点線、カード領域中央のX差が0px。仕事カードの右端から点線まで、点線から生活カードの左端までの距離が同値。時間レール左端・カードの時間位置・横収まり・画面位置保持・コンソールエラーに回帰がない。

## 2026-08-13｜中央線の座標一致だけでは描画ピクセルの一致にならなかった原因

- 依頼・診断: 見出し下の「仕事／生活」間の短い縦点線と、下の時間割中央の縦点線が見た目で合わない。
- 実測: PCでは見出し境界の右端とイベント層の中央線の計算位置がともにX=655.0、390pxではともにX=210.0だった。しかし上はspanのborder-right、下はイベント層のborder-leftで、同じ境界を異なる側の1pxで描いていた。
- 原因: DOMの矩形端を測る合格条件だけで、実際に線が塗られるピクセルの所有方向を確認していなかった。border-rightは境界の左側、border-leftは境界の右側へ描画されるため、座標値が同じでも線の見た目が1pxずれる。
- 再発防止: 境界線は左右で同じ疑似要素・同じborder方向・同じtransform規則から描画する。実測表には矩形座標だけでなく、線の所有要素、border方向、スクリーンショット上の実線Xを含める。
- 実装前の合格条件: 上下の線を同じ描画規則へ統一した後、PC／390pxでスクリーンショットの実線Xが一致し、矩形座標・カード内側余白・横収まり・コンソールエラーにも回帰がない。

### 再発訂正

- 右端固定のborder-leftへ置き換えただけでは、上の線が境界の左側へ塗られ、下の線と1pxずれた。
- 上下とも境界位置を基準にborder-leftを置き、同じtranslateX(-0.5px)で中央配置する規則へ変更した。
- 390pxの実スクリーンショットで上の線・下の線とも実線列X=201〜202となり、PC／390pxの横収まり・コンソールエラーも問題なかった。

### 再発訂正2（実装前診断）

- 実測: カード領域の幾何学的な中心と仕事・生活カードの余白は正しい（390pxはX=210、PCはX=655、左右余白5px）。一方、上下のborder-leftへ同じtranslateX(-0.5px)を適用したため、線の描画基準が390pxではX=209.5、PCではX=654.5になった。
- 原因: 1pxの左右差を直すための補正を、中心座標の補正としても重ねてしまった。線のピクセル一致と、線の幾何学的中心一致を同じ補正値で扱っている。
- 再発防止: カード端の距離と線の幾何学的中心を先に合格にし、描画補正は実線のラスタライズ結果を確認してから最小限にする。補正後は中心X・左右余白・スクリーンショットの実線列を同じ表で比較する。
- 実装前の合格条件: 390px／PC幅で中心線の幾何学的Xがカード領域中央と一致し、仕事右端→線、線→生活左端の視覚距離が同値になる。横収まり・位置保持・コンソールエラーに回帰がない。

### 実装後確認（0.31.22）

- [x] 上下の線からtranslateX(-0.5px)を外し、中心座標の補正を解除した。
- [x] 390pxで中心X=210、PC幅で中心X=655。仕事・生活カードの中心側余白は両幅とも5px。
- [x] 390px実スクリーンショットで上下の線が同じ実線列に揃い、横収まり・コンソールエラーに回帰がない。

## 2026-08-13｜時間割分類ボタンを3等分する

- 依頼・診断: 時間割の「仕事」「生活」「共通」は3つの同格な表示モードなのに、旧CSSが4列グリッドのまま残っており、3ボタンの枠内配分が不均等に見えた。
- 設計判断: 3モードの親をrepeat(3,minmax(0,1fr))の等分グリッドに固定する。ボタンは同じ最小高さ・同じ左右余白・同じ枠構造を使い、選択中の色だけを変える。
- 再発防止: 選択肢の数と親グリッドの列数を一致させる。選択肢を追加・削除したときは、PC／390pxの実表示で各ボタンの矩形幅を実測し、同値であることを合格条件に含める。
- 合格条件: PC／390pxで仕事・生活・共通のボタン幅が同値、外枠内に収まり、選択状態・色・タップ操作・横はみ出し・コンソールエラーに回帰がない。

## 2026-08-13｜3等分のコード修正が実表示へ反映されなかった原因

- 依頼・再発: ソース上の `.an-flow-filter` は3列へ修正済みだったが、実表示は4列のままで、3ボタンが左側3列に寄り右側が空いた。
- 原因: CSSファイルのURLが `?v=0.31.22` のまま変わらず、Service Worker／ブラウザキャッシュが修正前の `repeat(4, ...)` を返していた。ソース文字列の確認だけでは、実際に適用されたCSSを確認できていなかった。
- 設計判断: 画面コードの版と静的アセットのキャッシュキーを同じ版番号で管理し、CSS／JS／Service Workerの変更時は版を更新する。実表示では `getComputedStyle(...).gridTemplateColumns` と各ボタンの矩形を確認する。
- 再発防止: 変更後はソースだけでなく、実ブラウザの適用CSS・Service Workerキャッシュ・PC／390pxの矩形幅を確認する。3ボタンなら3列、3列の合計幅が親枠内幅と一致することを合格条件にする。
- 実装前の合格条件: PC／390pxで実際の適用グリッドが3列、3ボタンの幅が同値、親枠の右側に余白が残らず、コンソールエラーがないこと。

## 2026-08-13｜予定編集を選択後操作へ分離し、直前操作を戻せるようにする

- 依頼・診断: 現状は予定カードへのpointerdownだけで、カード上端・下端の小さな領域が開始／終了変更として即時に動作する。スマホではカードをタップしたつもりでも端の判定に入りやすく、誤移動と開始・終了の調整しづらさが同時に起きる。
- 設計判断: 未選択カードのpointerdownは選択だけにし、選択後だけカード中央の上下ドラッグを有効にする。選択後は「予定全体」「開始を調整」「終了を調整」を明示的に選べるため、端のつまみを必須にしない。詳細編集は選択後の操作欄から開く。
- 取り消し設計: 時間変更・詳細保存・削除の直前に予定の正本（plan／dailyTimeline／flowOverrides）を1世代だけスナップショットし、「直前の変更を戻す」で復元する。復元後は選択を解除し、同じ保存処理と表示位置保持を通す。
- 再発防止: 未選択状態でデータ変更を開始しない。操作モードと対象（全体／開始／終了）を画面上に表示し、PC／390pxでタップ選択、ドラッグ、端点調整、取り消しを実操作で確認する。
- 実装前の合格条件: 未選択カードのタップでデータが変わらず選択状態になる。選択後の各モードで30分単位に変更でき、端のつまみを正確に掴まなくても開始・終了を調整できる。移動直後に1回戻せ、位置保持・横収まり・コンソールエラーに回帰がない。

### 実装方針の具体化（直感操作）

- 利用者FB: 操作モードを並べたパネルではなく、カードをタップすると上下に出っ張るハンドルを直接掴みたい。長押しによる全体移動は残し、予定外をタップすると選択を解除したい。
- 変更判断: 選択時だけカード上端・下端のハンドルを表示する。上端は開始、下端は終了を調整し、カード中央の長押しは予定全体を移動する。未選択カードへのpointerdownでは移動を始めない。
- 再発防止: 変更対象を画面上の別パネルで説明するのではなく、操作対象そのものの状態（選択・ハンドル表示）で伝える。選択状態、ハンドルのヒット領域、予定外タップの解除をPC／390pxで確認する。
- 合格条件の追加: 選択中の上下ハンドルがカード外側から視認でき、ハンドル操作で開始／終了が30分単位に変わる。中央の長押し移動、予定外タップによる解除、直前の変更を戻すが回帰しない。

### 再発診断（0.31.25）

- 利用者報告: 1回移動した後、もう一度同方向へ動かせない。逆方向には動かせる。
- 実測: PCの同じ方向への連続移動は再現せず、2回とも30分ずつ移動できた。コードを追うと、タッチの中央長押し経路で開始タイマーがなく、pointermoveの10px超過時に移動開始前のpressを解除していた。
- 原因: 選択後のカード中央は「長押しで全体移動」という契約なのに、前回のハンドル切替修正時にタッチ用の430msタイマーを戻し忘れていた。ハンドル操作と中央長押しの開始条件を同じに扱っていた。
- 再発防止: 中央はPCの小移動／タッチの430ms長押し、上下ハンドルは即時開始と、開始条件を操作対象ごとに分離する。移動後は選択中の時刻も新しい正本の時刻へ更新し、次の操作が古い状態を参照しない。
- 修正後の合格条件: スマホ幅で同方向への連続移動ができ、逆方向もできる。中央長押しと上下ハンドルの開始条件が混線せず、移動後も選択・位置保持・取り消し・コンソールに回帰がない。

### 再発診断（ドラッグ後に詳細が開く）

0.31.30の実装で、編集操作をカード本体の`button`内に`span[role=button]`として置いた。PCの自動クリックでは動作したが、これは操作要素の入れ子であり、Safariやタッチ入力ではイベントの対象が外側カードへ再ターゲットされ、編集詳細が開かない再発余地がある。

PCを読み取り専用のまま運用する判断に合わせて確認したところ、時間割のドラッグ・開始端・終了端のpointerup処理に`canWrite()`相当の停止条件がなく、読み取り専用でも予定変更へ進める構造が残っていた。また、編集ボタンとハンドルは権限に関係なく選択後に表示されていた。

追加の再発では、予定カード本体にも`touch-action:none`を適用し、pointerdown直後からカードへpointer captureしていたため、予定上から始めた短い縦ドラッグがページスクロールへ渡らなかった。長押し移動の判定前にページ操作を止めていたことが原因である。

再発防止として、カード本体は縦スクロールを優先し、通常のpointerdownではpointer captureを取得しない。短い移動や長押し前の移動はページへ渡し、長押しタイマーが成立して予定移動を開始した瞬間だけcaptureと予定移動を有効にする。開始・終了ハンドルは明示的な時間調整操作なので、従来どおり個別に操作を優先する。

合格条件は、予定上から短い縦ドラッグを開始するとページが上下にスクロールすること、長押し後の予定移動は従来どおり30分単位で動くこと、選択・編集ボタン・端調整・読み取り専用制御が回帰しないこと、PC/390px実表示で画面位置とコンソールエラーが安定することとする。

再発防止として、読み取り専用は「表示・選択」だけを許可し、編集ボタン・上下ハンドルを描画しない。pointerdownとpointerupの両方で書き込み権限を確認し、途中で権限が変わっても保存しない。書き込み可能端末では、現在の選択→予定を編集、選択→ドラッグの導線を維持する。

合格条件は、PC読み取り専用で選択はできるが編集ボタン・ハンドル・ドラッグ変更・端調整・削除保存が発生しないこと、iPhone書き込み可能で編集ボタン・ハンドル・ドラッグ・詳細保存が動作すること、両方で画面位置とコンソールエラーが安定することとする。

実表示の再確認では、独立したbuttonにしても、タッチ経路によってclickの発火順や対象が変わる可能性が残った。そこで編集button自身のpointerupでも詳細表示を起動し、その直後のclickだけを消費する。クリックだけに依存せず、タッチの確定イベントを直接の入口にする。

再発防止として、予定カードは選択・ドラッグを担当する非ボタンの操作コンテナにし、詳細編集は独立した実`button`として子要素に置く。操作責務の異なる要素をHTML上でも入れ子のbuttonにしない。合格条件はPC/390pxの実表示で、選択後の「予定を編集」がタップ1回で詳細を開き、カード本体のタップ・移動・端調整とは干渉せず、コンソールエラーがないこととする。

今回の再発では、カードの2回目のclickを詳細表示に割り当てていたため、選択状態での予定移動と詳細編集の意図が衝突していた。また、選択中に編集へ進む明示的な操作対象がなく、カード下のドラッグや合成clickが詳細表示に見える状態になっていた。上側ハンドルもカード枠の上辺を基準にせず、固定値だけで上下を配置していたため、枠線の中心からずれた。

再発防止として、カード本体のタップは選択だけに限定し、詳細編集は選択中カード内の「編集」操作へ分離する。編集操作は1時間の最小カードでも見えるカード中央右に置き、カード本体の移動・端の調整と競合させない。取り消しは誤ドラッグから復帰できる安全弁として残すが、主操作から離し、表示名を「変更を取り消す」に統一する。上下ハンドルはカード上辺・下辺の枠線中心を共通基準にして配置する。

合格条件は、カード本体のタップで詳細が開かず選択状態になること、選択中の「編集」だけで詳細が開くこと、移動・端調整の後に詳細が開かないこと、1時間カードでも編集操作が見えること、上下ハンドル中心とカード枠線が一致すること、変更を取り消すが1回で復元すること、PC/390pxの実表示でスクロール位置とコンソールエラーに問題がないこととする。

前回の時間制限（700ms）では、端末やブラウザが遅れて発火させる合成clickを取りこぼす余地が残っていた。また、選択後の開始端・終了端の移動範囲が片方向にしか許可されておらず、時間を縮める操作ができなかった。

再発防止として、ドラッグ完了後は「次に発生する1回のclickだけを消費する」状態を共通の操作状態として持ち、合成clickが来ない場合は次のpointerdownで解除する。時間ではなくイベントの役割でドラッグとタップを分離する。端の変更は、開始端を`開始時刻〜終了時刻-30分`、終了端を`開始時刻+30分〜終了時刻`にクランプし、拡大・縮小を同じ境界規則で扱う。

合格条件は、PC/390pxの実表示で、移動・開始端・終了端のドラッグ後に詳細が開かないこと、通常のタップだけで詳細が開くこと、開始端・終了端の両方で30分単位の拡大・縮小ができること、選択状態・スクロール位置が失われないこと、コンソールエラーがないこととする。

- 利用者FB: ドラッグ後や「プラグイン開発」などの予定を移動した後は詳細を表示せず、詳細はタップ時だけ表示したい。
- 原因: pointerupで時間変更を保存した後、ブラウザが同じポインター操作から合成clickを発生させる。これを通常の「選択済みカードをタップして詳細を開く」と区別できず、ドラッグ直後に詳細が開いていた。従来の120ms抑制ではスマホのclick遅延を吸収できない。
- 設計判断: ドラッグ完了後は次に発生するカードclickを1回だけ消費し、通常タップの詳細導線には渡さない。外側タップによる選択解除はこの抑制の影響を受けない。抑制はclickが来ない場合に備えて短い期限で自動解除する。
- 再発防止: ドラッグとタップの入力終端を同じイベントとして扱わず、ドラッグ後の合成clickを明示的に消費する。PC／390pxで「ドラッグ→詳細なし」「タップ→詳細あり」「外側タップ→選択解除」を確認する。
- 合格条件: 予定全体・開始・終了のどのドラッグ後も詳細画面が開かず、通常のタップだけで詳細画面が開く。位置保持・取り消し・横収まり・コンソールエラーに回帰がない。

0.31.34の実機報告では、iPhoneの時間ピッカーで値を変更しても、保存前の画面に重ねている表示用要素が変更前の時刻を示し続けた。保存処理は入力値を正しく読み取るため保存後は反映されるが、表示同期が`input`イベントだけで、iOSのピッカー確定時に発火する`change`イベントを扱っていなかったことが原因である。

再発防止として、時間入力の表示は`input`と`change`を同じ表示同期関数へ集約し、ネイティブ入力の値と画面上の表示用要素を一つのDOM更新経路で同期する。合格条件は、PC／390px相当の実表示でピッカー確定直後に開始・終了の表示が変わり、保存後の時間割も同じ値になり、コンソールエラーがないこととする。

0.31.35の実表示で、体調記録の「記録日」枠がページ全体の中心より右に見えた。`an-health-date`を「ラベル幅＋残り幅」の2列gridにしていたため、日付枠が行全体ではなくラベルを除いた残り列の中心に配置されていたことが原因である。

再発防止として、ラベルは左の補助レールへ固定し、日付枠は行全体を基準に`flex`で幾何学的に中央配置する。日付枠の左右余白は同じ共通トークンで算出し、ラベルの長さや端末幅によって中心が動かないようにする。合格条件は、PC／390pxの実表示で日付枠の中心Xと体調記録行の中心Xが一致し、ラベルが枠に重ならず、画面位置・日付変更・コンソールエラーに回帰がないこととする。

0.31.36の実表示で、中央配置の新ルールが計算上は行中心と一致した一方、後段の共通`.an-health-date-control{width:100%}`が幅だけを上書きし、ラベルが日付枠へ重なる状態を確認した。原因は、同じ部品の幅定義が複数箇所に分散し、後勝ちのCSSを自動検査できていなかったことである。

再発防止として、日付枠の幅・中央配置は後段の最終共通ブロックで一度だけ確定し、先行定義があっても同じトークンで上書きする。合格条件に「ラベルが枠外の左レールに残ること」と「日付枠の中心Xと行中心Xの差が0px」を追加する。

毎日の予定は習慣として登録されていても、一定でない期間だけ存在する予定がある。利用者の依頼は「今日と明日は残し、明後日以降を終了する」であり、入金・体調・仕事記録とは無関係の時間割表示だけを対象とする。

原因と再発防止として、毎日の予定テンプレートを物理削除せず、`dailyTimelineEndDate`を正本データへ保存して表示境界を設ける。これにより対象日より後だけを非表示にし、誤操作時に終了日を戻せる。実行操作は書き込み可能端末だけに表示し、確認文で「明日まで残す」ことを明示する。合格条件は、今日・明日の毎日予定が残り、明後日以降に同じ予定が表示されず、他の予定・記録・入金データが変わらず、PC読み取り専用では変更操作が発生しないことである。

傷病手当の申請開始日・今回申請分の日数は、実際の入金ログや収入見込みと混ぜず、既存の受給期間（2026年1月開始・18か月）と同じ`benefit`領域に申請メタ情報として保持する。これを「今日使えるお金」パネルの裏面にまとめ、ワンタップで表裏を切り替える。入金記録は従来どおり銀行への着金時に行う。

再発防止として、金額面の表示・非表示と申請情報の表示を別責務にし、裏面には「次回申請開始日」「今回申請している分（月・日）」「受給予定期間」「残り月数」を固定順で表示する。設定画面で書き込み可能端末から申請メタ情報だけを変更できる。合格条件は、パネルのタップで詳細面へ切り替わり、金額を隠していても申請情報の意味が崩れず、保存後に再表示して値が一致し、PC読み取り専用・390px実表示・コンソールエラーに回帰がないことである。

0.31.38の実表示で、傷病手当パネルの表面に既存の`.an-summary-row`を入れたところ、親のカード構造と子の3列gridが重なり、アイコン・見出し・金額が縦方向へ崩れた。原因は、表裏カードを専用構造にせず、別責務の共通行コンポーネントを再利用したことである。また、旧データに新しい申請開始日キーが存在しない場合の空値補完も不足していた。

再発防止として、表裏カードは専用の内部gridを持ち、既存の一覧行レイアウトを子要素へ入れ子にしない。既存データに新しい任意項目がない場合は、正本の初期値をnormalizeと表示関数の両方で補完する。合格条件は、PC／390px実表示で表面のアイコン・説明・金額が横一列に収まり、裏面の4項目が読め、初期値8月1日と受給期間が表示され、コンソールエラーがないことである。

0.31.39の裏面実表示で、専用構造への分離後もカード高を表面の最小高86pxに固定していたため、裏面の4項目が次の残高行へ重なった。原因は表面と裏面で必要な情報量が異なるのに、共通の高さトークンを定義していなかったことである。

再発防止として、表裏を同じカード高トークンで管理し、最も情報量の多い裏面が収まる高さを基準にする。合格条件は、表面・裏面ともカード内に収まり、裏面の最下段と次の残高行の間に重なりがなく、390px実表示で読めることとする。

申請情報の入力欄を旧設定画面の重複した関数へ追加しても、実際の画面が参照する後段の設定関数に導線がなければ利用者へ届かない。原因は同名`settingsV2`の移行層が複数残っており、どの関数が最終描画入口かを確認せず追加したことである。

再発防止として、設定項目は最終的に`view`から呼ばれる設定関数へ追加し、自動検査で入力IDが実表示DOMに存在することを確認する。合格条件は、設定画面で申請情報の開閉・入力・保存ができ、裏面へ再表示されること、PC読み取り専用では保存が発生しないことである。

残り月数は、入金済みの印だけから計算すると、入金を銀行着金時に記録する運用では未入力期間を「受給残り」と誤認する。今回必要なのは、1月開始・18か月という受給対象期間のうち、今日の月以降が何か月残るかである。そこで裏面の残りは対象期間と現在月の比較で算出し、入金ログや`benefitMonths`とは分離する。

合格条件は、2026年1月開始・18か月の設定で2026年8月表示時に2026年8月〜2027年6月の11か月となり、入金記録の有無で残り月数が変動しないこととする。

### 0.31.43 支出・収入を記録画面の傷病手当情報パネル

0.31.42の実機報告では、タップ対象が「支出・収入を記録」画面の残高パネルだったが、傷病手当情報への反転導線は「お金の見通し」画面だけに実装されていたため反応しなかった。原因は、同じ意味を持つ残高パネルを画面ごとに別の操作対象として扱い、共通の操作入口を定義していなかったことである。

再発防止として、残高パネルは画面が変わっても「非ボタン領域を1タップすると傷病手当情報を開く、情報面を再タップすると残高へ戻る」という共通操作契約を持たせる。金額の表示・非表示ボタンは反転操作から分離し、誤って画面を切り替えないようにする。合格条件は、PC／390pxの実表示で記録画面の残高パネルを1タップすると「次回申請開始日」「今回申請している分」「受給予定期間」「残り」が表示され、再タップで通常表示へ戻り、隠す／表示ボタン単体では反転しないことである。

### 0.31.44 傷病手当情報の表示場所を「お金の見通し」に限定

利用者の実表示確認を受け、支出・収入の記録画面には傷病手当の申請情報を書かず、「お金の見通し」だけで確認する方針へ戻す。記録画面は支出・収入の入力に集中する画面であり、残高カードを別情報へ反転させると、記録と見通しの役割が混ざり、入力導線を見失いやすくなるためである。0.31.43の反転実装は、記録画面側だけを撤回し、見通し画面の反転と既存の金額表示／非表示は維持する。

再発防止として、画面ごとの責務を「記録＝金額・方法・カテゴリー」「見通し＝残高・借入・傷病手当の申請情報」として操作設計票に固定し、同じ残高表示に似た操作を画面横断で安易に共通化しない。合格条件は、PC／390pxの実表示で支出・収入画面に傷病手当の項目がなく、支出・収入の入力が従来どおり行え、お金の見通し画面だけで申請情報の反転が開閉できることである。

日報の外部ファイル自動取り込みは、任意フォルダの無断監視や既存の自動投稿フォルダとの混同を避けるため、専用の読み取り対象とファイル形式を先に決める。ブラウザの安全制約上、利用者が一度フォルダまたはファイルを許可する読み取り専用導線を基本とし、許可なしの常時監視やPCデータの上書きは行わない。合格条件は、読み取り元・対象日・取り込み内容を画面で確認でき、既存の日報を勝手に上書きせず、明示操作でのみ取り込めることである。
## 2026-08-13｜日報入力途中の保持・プロジェクト作成導線・時間レールの単一座標系

- 利用者報告: 仕事の内容など入力途中の値が、別アプリから戻ったときに消える。日報から新しいプロジェクトを作りたい。時間割にも開始・終了なしでプロジェクトだけを追加したい。時間割の時刻表示が中央からずれる。
- 原因: 日報の入力途中はDOMと一時メモリだけで、画面再構築・`visibilitychange`・`pagehide` 後に復元する端末内下書きがなかった。プロジェクト作成処理は時間指定の仕事追加にだけ存在し、日報と時間割で同じカタログ作成契約を使っていなかった。時間割は時間レール、イベント層、レーン境界、中央線に旧CSSの余白・左位置指定が残り、同じフレームのX座標を参照していなかった。
- 設計判断: 日報下書きはアプリ本体の同期データへ混ぜず、書き込み可能端末だけが使う端末内下書きキーへ保存する。明示保存で下書きを消し、PC読み取り専用端末では書き込まない。プロジェクト作成は共通の作成関数に集約し、日報の「プロジェクトを追加」と時間割の「プロジェクトだけを追加」から同じカタログへ登録する。時間割は時間レール幅・カード領域・中央線を共通CSSトークンで算出し、旧座標系の上書きを残さない。
- 再発防止: 入力をDOMだけの一時状態にしない。アプリ切替、日付変更、画面遷移、明示保存の各境界で下書きの保存・復元・削除を検査する。プロジェクト追加は日報・時間割の両方で同じデータ契約を通す。時間割の実表示では時刻ラベル、レーン見出し境界、中央線、カード左右端を同一スクリーンショットと実測値で比較し、PC幅と390px幅の両方で確認する。
- 実表示の合格条件: 390px相当とPC幅で、日報6項目・時刻・休憩・複数プロジェクトの入力途中が別画面から戻っても残る。明示保存後は下書きが消え、保存済み日報が表示される。読み取り専用PCは下書き・本体データを書き換えない。日報と時間割の「プロジェクトを選択」から新規プロジェクトを作成でき、時間割のプロジェクトだけ追加には開始・終了欄が出ない。時間レールとカード・中央線のX座標差は0px、横はみ出し・コンソールエラーがない。
## 2026-08-13｜プロジェクト選択肢の重複防止と時間割の自由記述

- 利用者報告: 「プロジェクトを選択」が選択肢としても表示され、実際の候補と重なって見える。「新しいプロジェクトを作る」は一覧の最後に置きたい。時間割は管理用の仕事内容選択ではなく予定なので、プロジェクト名の下の仕事内容を自由に記述したい。
- 原因: プレースホルダーを通常の`option`として表示していたため、未選択表示と選択肢一覧の役割が重複していた。新規プロジェクト項目も既存候補と同じグループに置かれていた。時間割の新規仕事欄は仕事カタログの優先度・状態入力を同じフォームに出し、予定の自由記述と管理情報の境界が曖昧だった。
- 設計判断: プレースホルダーは`disabled hidden`の表示専用とし、開いた候補一覧には出さない。既存プロジェクトを先に並べ、新規作成は最後に置く。時間割の新規予定はプロジェクトを選び、仕事内容を自由記述する。仕事一覧の優先度・状態は時間割の予定入力では必須にせず、既定値でカタログへ保存する。
- 再発防止: 選択欄は「表示用プレースホルダー」「既存候補」「新規作成」の3層を分け、すべての同種ドロップダウンへ同じ順序を適用する。時間割と仕事一覧の入力責務を分け、予定名・仕事内容・管理属性を別レイヤーとして設計する。390pxとPC幅で、選択欄を開いた状態・選択後・再描画後を実表示確認する。
- 実表示の合格条件: 日報と時間割のプロジェクト選択を開いても「プロジェクトを選択」が候補として重複せず、既存プロジェクトの後に「新しいプロジェクトを作る」が1件だけ表示される。時間割の新規予定でプロジェクト名と自由記述の仕事内容を入力でき、開始・終了・予定表示・保存後の再表示が一致する。横はみ出し・画面位置喪失・コンソールエラーがない。
## 2026-08-13｜対象範囲の訂正：時間割の「仕事を追加」は現状維持

- 利用者FB: 今回は時間割の「仕事を追加」の仕様変更は不要。変更対象は「仕事の記録」のプロジェクト選択欄だけとする。
- 設計判断: 時間割の既存追加導線・表示・保存構造は0.31.45のまま維持する。今回の変更は、仕事の記録側で表示用プレースホルダーを候補一覧から除き、既存プロジェクトの後に「新しいプロジェクトを作る」を置くことに限定する。
- 再発防止: 依頼の対象画面を実装前に設計ログと実表示確認項目で固定し、別画面へ同じ変更を広げない。対象外の導線を変更しないことも合格条件に含める。
- 合格条件: 仕事の記録の選択欄だけが、プレースホルダー非表示・既存候補先行・新規作成末尾になる。時間割の「仕事を追加」は変更前と同じ構造である。
## 2026-08-13｜日報のプロジェクト別自由記述と端末内下書き

- 原因：日報の「仕事内容」が予定カタログの選択欄と同じ値を使っており、一日の最後に実際に行った内容をプロジェクトごとに残せなかった。さらに、PC読み取り専用の判定が端末内の入力途中の下書き保存まで停止させていた。
- 判断：日報の各プロジェクト行は、予定の作業項目とは分離した自由記述欄とし、`workDescriptions[projectId]` でプロジェクト単位に保存する。PCの正式データは読み取り専用のまま、入力途中の下書きだけは端末内に保持する。
- 再発防止：予定カタログと実績記録のデータ責務を分け、読み取り専用判定を正式データ保存と下書き保存で共有しない。確認版では複数プロジェクトの入力、画面離脱後の復元、仕事内容の選択欄0件、横はみ出し0を実表示で確認する。
- 合格条件：プロジェクトAとBに別々の文章を入力しても互いに上書きされない。日報を離れて戻ったとき各文章が同じプロジェクト欄へ戻る。時間割の「仕事を追加」の導線は変わらない。
